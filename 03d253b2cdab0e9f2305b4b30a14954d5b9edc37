MM-ATS-02 review: PASS — modules/Row.lua 1446 -> 1226 (target <=1250) by moving the cell outline (BORDER_SIDES/BORDER_ANCHOR, cellBorderColor, borderThickness, hideFlatBorder, newBorderEdges, applyArtBorder, applyFlatBorder, Cell:ApplyBorder, Cell:ApplyEntryBorderColor) into modules/Row_Border.lua (256). Verified the move line by line: code identical, only four cross-reference comments reworded plus the new file header and NS.RowInternals resolution; Row.lua now publishes RGBA/ClassRGB/borderEdge and reaches the two methods only by name at call time, so the seam is real. TOC line sits right after modules\Row.lua (load-bearing, file-scope resolve). The static R3 getter scan in tests/test_row.lua widened to the new file (same case name). Ripple: ARCHITECTURE.md census/R3/band/peel count, module-map tree/table/load order, common-tasks and data-flow, RESULTS.md watch-list rows; MM-ATS-02R fixed the hub file counts (61->62, 24->25 modules) that the first commit left failing. CRLF on disk, LF in index. Gate re-run bounded at 03d253b: tests 2092 passed / 0 failed (2092 before, count preserved), luacheck 0/0 in 143 files, lizard nothing above CCN 15; largest authored Lua is modules/Aggregator.lua at 1432, nothing over 1500.
