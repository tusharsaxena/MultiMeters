  PASS  loadorder: every file the TOC names exists on disk
  PASS  loadorder: every shipped .lua on disk is named by the TOC
  PASS  loadorder: the TOC declares no duplicate file line
  PASS  loadorder: no file captures an NS symbol a later file publishes
  PASS  loadorder: locales/ loads ahead of every file that captures NS.L
  PASS  loadorder: the LibKa0s seams load in the order their headers pin
  PASS  loadorder: core/MultiMeters.lua loads after every core/ setup file
  PASS  layoutcap: every authored file over 1500 lines is named in the ARCHITECTURE.md census
  PASS  layoutcap: no census row outlives the breach it records
  PASS  layoutcap: every census row carries a disposition that can be followed
  PASS  complexityregister: the stated folder tally matches the table under it
  PASS  complexityregister: every row points at a file that still exists
  PASS  complexityregister: no function is entered twice
  PASS  complexityregister: every disposition can be followed
  PASS  every deviation id the register cites is assigned by a bundle in docs/audits/
  PASS  texturepaths: every hard-coded path in authored source is named in the ARCHITECTURE.md census
  PASS  texturepaths: no census row outlives the path it records
  PASS  texturepaths: every census row carries a disposition that can be followed
  PASS  texturepaths: the deviation register carries the row the ColumnBlocks sites point at
  PASS  texturepaths: the register row's ColumnBlocks citation names the lines the pair is on
  PASS  doc map: every Tier 2 row agrees with what docs/ holds
  PASS  docs/ARCHITECTURE.md carries the ten sections documentation-§3 names
  PASS  every mandated hub section that has a topic doc has spilled into it
  PASS  every anchor pointing into docs/ARCHITECTURE.md resolves to a heading
  PASS  the player-facing history has the ONE home documentation-§1 allows, and no second
  PASS  README.md's top-level sections are the ones documentation-§1 names, in its order
  PASS  lintconfig: .luacheckrc sets no top-level ignore
  PASS  lintconfig: .luacheckrc switches no warning class off wholesale
  PASS  lintconfig: every files[...] ignore is narrowed to a file or a name
  PASS  lintconfig: no source file carries a bare inline luacheck ignore
  PASS  Constants: NS.Const and NS.Constants are the same table
  PASS  Constants: the chat prefix is the cyan [MM] tag and closes its color code
  PASS  Constants: the notice gray is a bare color opener with no closer
  PASS  Constants: the monospace font comes from the LibKa0s payload
  PASS  Constants: the font it names exists in the vendored payload
  PASS  Constants: the LSM font key is a name, not the path
  PASS  Constants: every STATS row is fully populated and correctly typed
  PASS  Constants: no two STATS rows share a key or an enum value
  PASS  Constants: STAT_BY_KEY holds exactly the catalog, by identity
  PASS  Constants: EnemyDamageTaken is READABLE but is NOT a column
  PASS  Constants: READABLE_STAT_BY_KEY is a superset of STAT_BY_KEY
  PASS  Constants: each stat key is the name of the enum value it carries
  PASS  Constants: DEFAULT_STAT_KEYS is derived from defaultEnabled, in catalog order
  PASS  Constants: the six default columns are the design's six
  PASS  Constants: only the two rate stats carry isRate
  PASS  Constants: every stat label and short label has an enUS entry
  PASS  Constants: the resolved enums match the client's when it has them
  PASS  Constants: the hardcoded fallbacks equal the live enum values
  PASS  Constants: Dps and Hps are deliberately absent from STAT_TYPE
  PASS  Constants: every bus message is uniquely named under the addon's prefix
  PASS  Constants: every declared bus message is sent somewhere in the addon
  PASS  Constants: the throttle window is a real range around the shipped default
  PASS  Constants: the row cap covers a full raid and the pool step covers a party
  PASS  Secrets: core/Secrets.lua is the only file that names a detection API
  PASS  Secrets: IsRestricted tracks the client's restriction state
  PASS  Secrets: IsRestricted answers a plain boolean, never the API's raw return
  PASS  Secrets: with no C_RestrictedActions, IsRestricted falls back to combat lockdown
  PASS  Secrets: with neither API nor lockdown, IsRestricted is false rather than an error
  PASS  Secrets: a raising IsAddOnRestrictionActive degrades instead of propagating
  PASS  Secrets: GetRestrictionState forwards the raw state, including Activating
  PASS  Secrets: GetRestrictionState answers nil — not a state — when the API is absent
  PASS  Secrets: STATE republishes the client's enum, and falls back to the documented literals
  PASS  Secrets: IsSecret is true for a secret and false for every plain value
  PASS  Secrets: IsSecret answers a boolean, not the client's raw return
  PASS  Secrets: CanAccess permits plain values and refuses an inaccessible secret
  PASS  Secrets: CanAccess permits a secret during the Activating edge
  PASS  Secrets: CanCompare refuses a secret and permits a plain value
  PASS  Secrets: CanCompare2 refuses when EITHER operand is secret
  PASS  Secrets: IsSafeKey refuses a secret even when this context may READ it
  PASS  Secrets: no inspector ever compares or measures the value it is handed
  PASS  Secrets: IsSecretTable distinguishes a secret table from a table of secrets
  PASS  Secrets: CanAccessTable refuses everything that is not a table
  PASS  Secrets: CanAccessTable refuses a secret table under enforcement
  PASS  SafeIterate: walks an ordinary array in order and reports how many it visited
  PASS  SafeIterate: stops at the first nil rather than running to the limit
  PASS  SafeIterate: never applies the length operator to the table
  PASS  SafeIterate: a secret table is refused before it is ever indexed
  PASS  SafeIterate: hands secret VALUES to the callback untouched
  PASS  SafeIterate: a callback returning false stops the walk
  PASS  SafeIterate: a callback returning nil does NOT stop the walk
  PASS  SafeIterate: bad arguments are refused rather than raising
  PASS  SafeIterate: an unbounded array stops at the hard ceiling
  PASS  SafeCount: counts an ordinary array and stops at the first nil
  PASS  SafeCount: never applies the length operator
  PASS  SafeCount: answers nil — not 0 — when the count is not obtainable
  PASS  SafeCount: counts a table of secret values without touching one
  PASS  SafeCount and SafeIterate agree on the same array
  PASS  Secrets degraded: with no detection APIs, nothing is secret and everything compares
  PASS  Secrets degraded: SafeIterate and SafeCount are ordinary array walks
  PASS  Secrets degraded: canaccessvalue alone missing still refuses a known secret
  PASS  Secrets degraded: canaccesstable alone missing still refuses a secret table
  PASS  Compat: GetSpellInfo flattens C_Spell's struct to the old multi-return
  PASS  Compat: GetSpellInfo answers nil for an unknown spell rather than raising
  PASS  Compat: GetSpellInfo and GetSpellTexture fall back to the bare globals
  PASS  Compat: the spell shims answer nil with no API at all
  PASS  Compat: the spec shims prefer C_SpecializationInfo and fall back to the globals
  PASS  Compat: the spec shims answer nil with no API at all
  PASS  Compat: IsDamageMeterAvailable forwards Blizzard's own failure reason verbatim
  PASS  Compat: with no C_DamageMeter, IsDamageMeterAvailable is false and the addon still loads
  PASS  Compat: every session shim answers nil with no C_DamageMeter
  PASS  Compat: GetAvailableCombatSessions answers an EMPTY TABLE, never nil
  PASS  Compat: ResetAllCombatSessions reports whether the call was actually made
  PASS  Compat: a half-present C_DamageMeter degrades per function
  PASS  Compat: every meter shim survives a fully secret session
  PASS  Compat: the join key comes back SECRET under the restriction
  PASS  Compat: the optional source arguments are forwarded, never defaulted
  PASS  Compat: CreateNumericRuleFormatter does NOT abbreviate on its own
  PASS  Compat: CreateAbbreviatedNumberFormatter is the one that abbreviates
  PASS  Compat: the client's default breakpoints are reachable as a fallback
  PASS  Compat: CreateNumericRuleFormatter answers nil rather than a Lua lookalike
  PASS  Compat: no file in this addon divides a meter value
  PASS  Compat: the recap shims answer safely on a client with no C_DeathRecap
  PASS  Compat: a namespace present but missing one member does not take the rest down
  PASS  Compat: a recap call that raises is a nil answer, not a raise
  PASS  Compat: HasRecapEvents is a PLAIN boolean, whatever the client returns
  PASS  Compat: the recap shims never inspect what they carry
  PASS  Compat.HasDeathRecap keys on the EVENTS, not on the namespace
  PASS  Compat.IsInDelve answers on any ONE rung of the ladder
  PASS  Compat.IsInDelve is false in an ordinary scenario
  PASS  Compat.IsInDelve is false on a client with none of the delve APIs
  PASS  Compat.IsSkyriding reads glide CAPABILITY, not altitude
  PASS  Compat.IsSkyriding is a PLAIN boolean, and false with no C_PlayerInfo
  PASS  Compat.IsInHousing follows C_Housing, and is false without it
  PASS  Compat: a delve namespace present but missing its member does not raise
  PASS  State: every flag starts at its shipped default on a fresh load
  PASS  State: no state flag leaks into the profile defaults tree
  PASS  State: the SavedVariables globals never carry a state flag after a full load
  PASS  State: SetRestricted normalizes to a plain boolean
  PASS  State: core/Secrets.lua stays the authority, and State is only its mirror
  PASS  State: only core/MultiMeters.lua writes the restriction mirror
  PASS  State: SetTestMode flips the flag and publishes TEST_MODE_CHANGED once
  PASS  State: SetTestMode no-ops when the flag is already in the requested state
  PASS  State: SetTestMode coerces truthy values to a boolean before comparing
  PASS  State: SetTestMode is the only sender of TEST_MODE_CHANGED
  PASS  State: SetActiveWindow moves the pointer and publishes NOTHING
  PASS  State: Cache creates a named sub-table on first use and returns the same one after
  PASS  State: two modules' caches are independent
  PASS  State: WipeCache empties one cache IN PLACE, keeping the caller's upvalue live
  PASS  State: WipeCache with no name empties every cache, still in place
  PASS  State: WipeCache on a cache that was never created is a no-op, not an error
  PASS  State: loading core/State.lua creates no frame and registers no game event
  PASS  Locale: a missing key returns the key itself
  PASS  Locale: the fallback never returns nil, for any key shape
  PASS  Locale: NS.L is published before any consumer can read it
  PASS  Locale: every declared entry is a non-empty string
  PASS  Locale: no key is declared twice
  PASS  Locale: every translated value carries the same format specifiers as its key
  PASS  Locale: every stat label and short label in the catalog is declared
  PASS  Locale: keys are the English strings, not opaque identifiers
  PASS  Locale: NS.L is never handed to a LibKa0s descriptor as its `L`
  PASS  Locale: the locale file registers no second table over NS.L
  PASS  Locale: enUS is the only locale shipped, and it is unconditional
  PASS  Database: InitDB publishes the live instance under both names
  PASS  Database: the profile is the SHARED Default, not a per-character one
  PASS  Database: with AceDB absent, InitDB says so and leaves NS.db nil
  PASS  Database: a stored false, "" and 0 all survive the window shape merge
  PASS  Database: the same false survives a REAL login — SavedVariables to merged profile
  PASS  Database: the merge fills every key the stored window is missing
  PASS  Database: the merge deep-copies, so no window shares a sub-table with the template
  PASS  Database: the three profile callbacks are registered in the method-name form
  PASS  Database: a stored columns array is left exactly as the user ordered it
  PASS  Database: an ABSENT columns array becomes an empty array, never nil
  PASS  Database: EnsureWindowShape is idempotent
  PASS  Database: EnsureWindowShape refuses a non-table without raising
  PASS  Database: GetWindows answers an empty table before the database is up
  PASS  Database: a fresh profile is seeded with exactly one window
  PASS  Database: the seed window is NOT an AceDB default, so a deleted last window stays deleted
  PASS  Database: FindWindow answers the window and its index, and nil for anything else
  PASS  Database: window ids are monotonic and never reused
  PASS  Database: NextWindowId answers 1 with no database rather than raising
  PASS  Database: a stored window with no id is given one rather than dropped
  PASS  Database: RunMigrations stamps and holds the current schema version
  PASS  Database: the schema version is account-wide, not per-profile
  PASS  Database: a version ahead of any registered step is walked forward, not spun on
  PASS  Database v1: a v1 account walks all the way to the catalog shape
  PASS  Database v2: the frame is widened to hold the new grid
  PASS  Database v2: a frame that already fits the grid is left alone
  PASS  Database v2: a frame already wider than the grid is left alone
  PASS  Database: EVERY saved profile is walked, not just the active one
  PASS  Database v2: the step is idempotent and survives a malformed window
  PASS  Database: RunMigrations with no database is a no-op, not an error
  PASS  Database: RunMigrations normalizes every window whatever the version claims
  PASS  Database: a profile swap publishes PROFILE_CHANGED exactly once, with the new key
  PASS  Database: core/Database.lua is the only sender of PROFILE_CHANGED
  PASS  Database: a profile swap clears the session state derived from the old profile
  PASS  Database: the profile a swap lands on is migrated and normalized before anything reads it
  PASS  Database: a profile RESET re-seeds rather than leaving an empty registry
  PASS  Database v3: ANY of the three old icon flags means the icon stays on
  PASS  Database v3: all three off stays off
  PASS  Database v3: the three dead keys are REMOVED, not left to rot
  PASS  Database v4: a stored AUTO channel folds to SELF, in EVERY profile
  PASS  Database v4: every other channel is left exactly as the player set it
  PASS  Database v4: a profile with no export block at all survives the step
  PASS  Database v5: the FIRST window's values are the ones lifted
  PASS  Database v5: the per-window keys are REMOVED from EVERY window
  PASS  Database v5: the window's value beats whatever sits at the profile address
  PASS  Database v5: a profile whose windows never carried the keys survives
  PASS  Database v6: the two dead row-background keys are pruned from every window
  PASS  Database v7: a class-colour boolean becomes a colour mode, on every surface
  PASS  Database v7: a window that never set one is left to the shipped default
  PASS  Database v8: the four redundant header keys are pruned from every window
  PASS  Database v8: a typed header title becomes the window's NAME, not nothing
  PASS  Database v8: a window that was named itself keeps its own name
  PASS  Database v9: the title bar's background mode is pruned, the column strip's is kept
  PASS  Database v10: a stored cursor anchor becomes TOP, and other anchors are left alone
  PASS  Database: v12 -> v13 moves the title-bar toggle onto the header
  PASS  Database: v12 -> v13 leaves a window that never stored the toggle alone
  PASS  Database: v12 -> v13 turns the control class-colour flags into modes
  PASS  Database v2: the widening uses the window's OWN padding, not the template's
  PASS  Database v2: a frame with no numeric width is given one, and every window gets its own
  PASS  Database v5: a stored false and a stored 0 are lifted, not read as unset
  PASS  Database v5: only the key the window actually carried is lifted
  PASS  Database v5: only the FIRST window is consulted, even when it carries neither key
  PASS  Database v5: a first window whose data block is not a table lifts nothing
  PASS  Database v5: EVERY profile lifts from its OWN first window
  PASS  Database: v12 -> v13 keeps the rest of an existing header block
  PASS  Database: v12 -> v13 overwrites a control colour mode that was already there
  PASS  Database: v12 -> v13 maps each control flag on its own
  PASS  Database: v12 -> v13 leaves a window with no frame block at all alone
  PASS  Database: v12 -> v13 walks every saved profile, not just the active one
  PASS  Diagnostics: the report is published and reachable
  PASS  Diagnostics: `/mm debug diag` reaches it without the debug log
  PASS  Diagnostics: every section appears
  PASS  Diagnostics: it reports what the CLIENT has, not what the addon wants
  PASS  Diagnostics: a number that misses the ladder is FLAGGED, not just printed
  PASS  Diagnostics: one broken section cannot take the report down
  PASS  Diagnostics: it never renders a meter value
  PASS  Diagnostics: with no window it says so rather than erroring
  PASS  Diagnostics: the report lands in the debug console, not in chat
  PASS  Diagnostics: the console is OPENED, so the report is not written out of sight
  PASS  Diagnostics: with no console the report falls back to chat
  PASS  Diagnostics: a font size read back as 10.000000953674 is not called a failure
  PASS  Diagnostics: a font the layout reverted is named as such
  PASS  Diagnostics: a walk that never reached a spell does not blame the build
  PASS  Diagnostics: the number probes expect what the SHIPPING ladder renders
  PASS  Diagnostics: the enemy column's display types are printed, not assumed
  PASS  Diagnostics: an enemy flagged None is called out, because it defeats the class gate
  PASS  Diagnostics: a display-type check that could not run says so
  PASS  Diagnostics: the provider-order probe reports a RANKED column as ranked
  PASS  Diagnostics: the probe NAMES the position where the order breaks
  PASS  Diagnostics: the probe REFUSES mid-pull rather than reporting a false all-clear
  PASS  Diagnostics: `/mm debug recap` reaches the probe without the debug log
  PASS  Diagnostics: the probe lists EVERY death, not just the newest per player
  PASS  Diagnostics: it probes a NON-LOCAL id and an OLDER id, not only the newest
  PASS  Diagnostics: a client with no reader is the answer that RE-SCOPES the issue
  PASS  Diagnostics: a reader that refuses an id is reported, not swallowed
  PASS  Diagnostics: the probe survives a client that answers nothing at all
  PASS  Diagnostics: a secret id is described rather than called
  PASS  Diagnostics: the recap probe also rides along in the full report
  PASS  Diagnostics: the probe prints the whole namespace surface, unfiltered
  PASS  Diagnostics: an absent namespace reads absent, not empty
  PASS  Diagnostics: it says WHICH search found each reader
  PASS  Diagnostics: with both searches empty the verdict says so CONCLUSIVELY
  PASS  Diagnostics: a reader that answers NOTHING is not a green light
  PASS  Diagnostics: the verdict names WHICH of the four slots answered
  PASS  Diagnostics: a reader found by name is actually CALLED
  PASS  Diagnostics: an ARRAY of events is described as one, with its fields
  PASS  Diagnostics: an empty array is not mistaken for an answer
  PASS  Diagnostics: a slot with no death is not counted as a slot that refused
  PASS  Diagnostics: a slot that WAS probed and refused still raises the warning
  PASS  Diagnostics: the recap probe reports why a death is dated the way it is
  PASS  Diagnostics: the dating header prints the format the window is ACTUALLY set to
  PASS  Diagnostics: the formatter is resolved as Numbers first, and asked for both styles by name
  PASS  Diagnostics: with NS.Numbers gone the dating falls through to NS.Format
  PASS  Diagnostics: with no formatter at all it says so and dates nothing
  PASS  Diagnostics: the dating stops at four deaths
  PASS  Diagnostics: a secret id costs the row its dating, and still spends one of the four
  PASS  Diagnostics: a death the client holds no recap for still gets its row
  PASS  Diagnostics: the death is dated off the NEWEST event, which is events[1]
  PASS  Diagnostics: the header section covers every control, by walking them
  PASS  Diagnostics: a window with no controls says so rather than printing nothing
  PASS  Diagnostics: the identity report is published and reachable
  PASS  Diagnostics: `/mm debug identity` reaches it without the debug log
  PASS  Diagnostics: the identity report prints the last pass's rectangle
  PASS  Diagnostics: the identity report prints the rows-per-key histogram
  PASS  Diagnostics: the identity report prints the seats of every collided key
  PASS  Diagnostics: the identity report asks whether a SECRET GUID resolves
  PASS  Diagnostics: the lookup section draws the verdict rather than leaving counts
  PASS  Diagnostics: out of combat the lookup section refuses to draw a verdict
  PASS  Diagnostics: the identity report audits what the CLIENT annotates secret
  PASS  Diagnostics: an un-audited field is named as a CANDIDATE for the key
  PASS  Diagnostics: with NO field outside the key, the audit closes the direction
  PASS  Diagnostics: with no identity pass measured, the report says so honestly
  PASS  Diagnostics: the identity report survives a provider that answers nothing
  PASS  Diagnostics: an ABSENT field the addon reads is called out, loudly
  PASS  Diagnostics: a rectangle from a pull that has ENDED is marked stale
  PASS  Diagnostics: a field on SOME rows reads PARTIAL, and names the local row
  PASS  Diagnostics: a candidate with ONE value across the raid is called useless
  PASS  Diagnostics: a candidate that DOES vary is called out as worth trying
  PASS  Diagnostics: the audit says how many rows it sampled
  PASS  Diagnostics: a missing isLocalPlayer is NOT called a degraded key
  PASS  Diagnostics: the feign report is published and reachable
  PASS  Diagnostics: the feign trace records nothing until it is armed
  PASS  Diagnostics: an armed trace records the cast and the unit token it arrived under
  PASS  Diagnostics: an armed trace records what prune saw and what it decided
  PASS  Diagnostics: the entry that simply LEFT THE GROUP says so, instead of going quiet
  PASS  Diagnostics: an evicted entry still reads noted or down, never <evicted>
  PASS  Diagnostics: arming a trace clears the one before it
  PASS  Diagnostics: an empty armed trace is reported as a FINDING, not as a failure
  PASS  Diagnostics: the feign report prints the group beside the trace
  PASS  Diagnostics: the feign report survives a client with none of the unit APIs
  PASS  Diagnostics: a secret GUID costs one field and not the line
  PASS  Diagnostics: every roster row carries all six fields, in one fixed order
  PASS  Diagnostics: with no group the roster says so instead of printing a bare header
  PASS  Diagnostics: a unit read that REFUSES is named, and the row survives it
  PASS  Diagnostics: the roster is printed BELOW the entries, on the armed path too
  PASS  Diagnostics: a judge row for a GUID no cast line named is counted, not recorded
  PASS  Diagnostics: a judge row for a GUID a cast line named is recorded
  PASS  Diagnostics: a cast line survives a full ring of judge rows
  PASS  Diagnostics: the ring keeps its newest entries and reads them oldest first
  PASS  Defaults: DefaultWindow stamps the id and the name it is given
  PASS  Defaults: DefaultWindow falls back to the template's name
  PASS  Defaults: two windows share NO sub-table, at any depth
  PASS  Defaults: a window shares NO sub-table with the shipped template
  PASS  Defaults: editing one window leaves the other and the template untouched
  PASS  Defaults: a new window carries the WHOLE catalog, defaults ticked and first
  PASS  Defaults: no column carries a width or a show-bar flag
  PASS  Defaults: two windows' column entries are separate tables
  PASS  Defaults: the template carries every group the settings pages edit
  PASS  Defaults: no template leaf is nil, and no leaf is a function
  PASS  Defaults: every stored color is a keyed RGBA table with all four channels
  PASS  Defaults: the grid and the header ship the SAME face
  PASS  Defaults: the shipped fonts are LSM keys, not paths
  PASS  Defaults: the shipped sort column is a stat the window ships enabled
  PASS  Defaults: the shipped session type is Overall, which is never empty
  PASS  Defaults: the shipped sort mode is `value`, which R2 may fall back from
  PASS  Defaults: the shipped position is stored, never read back off a frame
  PASS  Defaults: the profile itself is nearly empty — almost everything is per-window
  PASS  Defaults: the shipped registry is empty and the id counter starts at 1
  PASS  Defaults: the minimap table uses LibDBIcon's own `hide` key
  PASS  Defaults: global carries only what is genuinely account-wide
  PASS  Defaults: the remembered roster ships EMPTY and with both of its maps
  PASS  Defaults: NS.C aliases the profile defaults rather than copying them
  PASS  Defaults: the debug flag is NOT a profile default
  PASS  CoreSetup: the harness loads the vendored LibKa0s majors, so nothing measures a stub
  PASS  CoreSetup: the runner FEEDS the derived library list, and it is not empty
  PASS  CoreSetup: the TOC-derived addon list leaks no libs/ entry
  PASS  CoreSetup: the suite list and tests/test_*.lua on disk agree in both directions
  PASS  CoreSetup: NS.Print and NS.Util.print are the SAME function object
  PASS  CoreSetup: the printer survives AceConsole's embed
  PASS  CoreSetup: the prefix is re-read on every call, not frozen at load
  PASS  CoreSetup: NS.Format renders through the same printer
  PASS  CoreSetup: IsConcatSafe rejects a value table.concat would raise on
  PASS  CoreSetup: SafeToString renders a secret as the sentinel rather than raising
  PASS  CoreSetup: the secret-safe members are the library's own, published by reference
  PASS  CoreSetup: RGBA reads the keyed shape the profile ships
  PASS  CoreSetup: RGBA reads the positional shape the options color widget writes
  PASS  CoreSetup: a stored ZERO channel survives as zero
  PASS  CoreSetup: presence of any of r/g/b makes the keyed shape win for all four
  PASS  CoreSetup: channels fall back independently, so a three-element color keeps its alpha
  PASS  CoreSetup: RGBA answers the four defaults for a non-table
  PASS  CoreSetup: the fallback color reader stands behind the library's
  PASS  CoreSetup: the class colour is the LIBRARY's one resolver, not a private copy
  PASS  CoreSetup: the classFilename reader is KEPT, because the library has no equivalent
  PASS  CoreSetup: the class colour degrades to a working reader, not to nothing
  PASS  CoreSetup: the window edge comes from the library, never from a private lookalike
  PASS  CoreSetup: the close button is the library's, told which addon is asking
  PASS  CoreSetup: no addon file restates a Core.SKIN value
  PASS  CoreSetup: NS.LIBKA0S_MISSING is set on BOTH paths, not only the degraded one
  PASS  CoreSetup: all five seams append to the shared clause rather than re-spelling it
  PASS  PerfSetup: NS.Perf is the library instance, with the gate as a plain boolean field
  PASS  PerfSetup: the capture ring is a SECOND SavedVariables global, outside the AceDB tree
  PASS  PerfSetup: the capture record is stamped from the TOC manifest
  PASS  PerfSetup: the manifest is read through NS.Version, never by naming C_AddOns
  PASS  PerfSetup: no locale table is handed to the library
  PASS  PerfSetup: the descriptor names the FOLDER and leaves the close control to the library
  PASS  PerfSetup: every declared bucket is reached by a real bracket in the addon's source
  PASS  PerfSetup: every bracket in the addon names a bucket the descriptor declares
  PASS  PerfSetup: the bucket nesting is declared, so a reader never sums a parent with a child
  PASS  PerfSetup: every instrumented module takes the probe as a file-scope upvalue
  PASS  PerfSetup: every bracket is gated, so an unstarted capture costs one boolean read
  PASS  PerfSetup: perf output is deliberately NOT gated on the debug flag
  PASS  PerfSetup: suspend stops the provider ASKING the meter, not just discarding the answer
  PASS  PerfSetup: suspend takes the provider's bus subscriptions down
  PASS  PerfSetup: the show decision refuses every window while suspended, above the master enable
  PASS  PerfSetup: resume restores from CURRENT state, not from a pre-suspend snapshot
  PASS  PerfSetup: suspend and resume are idempotent
  PASS  PerfSetup: the descriptor resolves its modules at CALL time
  PASS  PerfSetup: with LibKa0s absent the stub answers every member the addon reaches
  PASS  PerfSetup: the degraded `/mm perf` answers with the shared cause and its own consequence
  PASS  DebugLogSetup: NS.DebugLog is the library instance and NS.Debug is its bare sink
  PASS  DebugLogSetup: the sink is gated on the flag and costs nothing when it is off
  PASS  DebugLogSetup: a SECRET reaching a log line renders as the sentinel, never as a raise
  PASS  DebugLogSetup: the flag stays the addon's — the library holds no second copy
  PASS  DebugLogSetup: the debug flag never reaches SavedVariables
  PASS  DebugLogSetup: enabling the console acknowledges in chat
  PASS  DebugLogSetup: the [Init] summary names the version, the schema and the profile
  PASS  DebugLogSetup: the console takes the shipped monospace font by PATH
  PASS  DebugLogSetup: the font is registered with LSM exactly once, and not from this file
  PASS  DebugLogSetup: the library is told the FOLDER name, not just the frame name
  PASS  DebugLogSetup: the frame names are seeded from the addon name
  PASS  DebugLogSetup: the console's visibility change refreshes an open settings panel
  PASS  DebugLogSetup degraded: the stub carries the WHOLE live surface
  PASS  DebugLogSetup degraded: NS.Debug is still a plain callable function
  PASS  DebugLogSetup degraded: the flag still works, and says so once
  PASS  DebugLogSetup degraded: the honest missing-console line is said ONCE
  PASS  DebugLogSetup degraded: the stub reproduces NO part of the library's line format
  PASS  DebugLogSetup degraded: the console checkbox answers a usable data contract
  PASS  DebugLogSetup degraded: the buffer introspection answers rather than erroring
  PASS  DebugSteady: an unchanged pass emits once, not once per pass
  PASS  DebugSteady: a CHANGE emits on the pass it happens, never delayed
  PASS  DebugSteady: the run's count lands on the line it describes
  PASS  DebugSteady: the (xN) line keeps EVERY field, not just the first
  PASS  DebugSteady: two windows sharing a tag do not defeat each other
  PASS  DebugSteady: an unchanged run re-announces itself, so silence still means something
  PASS  DebugSteady: a SECRET argument is emitted at once and never replayed
  PASS  DebugSteady: it exists without the library, because the render path calls it
  PASS  DebugSteady: a reset makes the next pass speak again
  PASS  DebugSteady: toggling the flag forgets every run
  PASS  DebugSteady: two call sites under ONE tag do not defeat each other
  PASS  MediaSetup: NS.Icon answers the vendored path, extensionless
  PASS  MediaSetup: an icon the library does not ship answers nil
  PASS  MediaSetup: NS.MediaFont answers the vendored face
  PASS  MediaSetup: the font this addon names is the face the library registers
  PASS  MediaSetup: every icon the header strip draws is one the library ships
  PASS  MediaSetup: every name the library ships has a file in the vendored copy
  PASS  MediaSetup: with no library there is no art, and that is not an error
  PASS  EnvSetup: the vendored library really did register, so the cases below mean something
  PASS  EnvSetup: NS.Meta reads this addon's TOC
  PASS  EnvSetup: NS.Meta asks about THIS addon's folder, not its title or its chat tag
  PASS  EnvSetup: NS.Meta answers nil — not a placeholder — for a field the TOC does not carry
  PASS  EnvSetup: NS.Version prefers the TOC over this addon's own constant
  PASS  EnvSetup: NS.Version falls back to this addon's own constant
  PASS  EnvSetup degraded: an install with no LibKa0s still reads its own TOC
  PASS  EnvSetup: the deleted shim is gone from Compat
  PASS  EnvSetup: the deprecated bare global is still a live rung, all the way to NS.version
  PASS  EnvSetup: with no reader at all, core/Namespace.lua takes its own FALLBACK_VERSION
  PASS  EnvSetup: the version was resolved at load, not deferred
  PASS  Lifecycle: NS IS the AceAddon object, promoted in place
  PASS  Lifecycle: every module registers, and the enable cascade runs them all
  PASS  Lifecycle: every module is also published under its flat NS name
  PASS  Lifecycle: the AceConsole embed is reclaimed on the line after NewAddon
  PASS  Lifecycle: OnInitialize builds the database FIRST
  PASS  Lifecycle: OnInitialize runs end to end on a fresh client
  PASS  Lifecycle: with the settings layer gone, `/mm` is claimed anyway and says why
  PASS  Lifecycle: OnEnable registers exactly the events the fan-out handles
  PASS  Lifecycle: no module registers a game event of its own
  PASS  Lifecycle: both combat edges fan out as one message carrying nothing
  PASS  Lifecycle: every player-state edge fans out as PLAYER_STATE_CHANGED
  PASS  Lifecycle: a client with no PLAYER_IS_GLIDING_CHANGED still enables
  PASS  Lifecycle: PLAYER_ENTERING_WORLD is republished with its login/reload flags
  PASS  Lifecycle: the roster cache is dropped BEFORE ROSTER_CHANGED goes out
  PASS  Lifecycle: a meter reset wipes EVERY cache before publishing
  PASS  Lifecycle: the session event forwards its payload, which is never secret
  PASS  Lifecycle: the meter update event is a bare republication
  PASS  Lifecycle: the three meter handlers carry the meterEvent bracket
  PASS  Lifecycle: OnEnable seeds the restriction mirror from the LIVE state
  PASS  Lifecycle: the restriction event updates the mirror and forwards the RAW state
  PASS  ShouldShow: the ladder answers a reason that names the step that decided
  PASS  ShouldShow: a non-table is refused before anything else is consulted
  PASS  ShouldShow: the master enable refuses every window
  PASS  ShouldShow: General visibility set to Never refuses every window
  PASS  ShouldShow: General visibility's two combat answers follow the pull
  PASS  ShouldShow: General visibility is read BELOW test mode, so a layout can still be made
  PASS  ShouldShow: test mode overrides context, so a window can be positioned anywhere
  PASS  ShouldShow: the context rules are Visibility's, consulted rather than reimplemented
  PASS  ShouldShow: a missing Visibility module fails OPEN
  PASS  libs/LibKa0s is the LibKa0s release CLAUDE.md says this addon bundles
  PASS  tests/_kit is the test kit that shipped with that release
  PASS  Format: NS.Format is a callable table carrying both contracts
  PASS  Format.Number goes through the native ABBREVIATING formatter
  PASS  Format.Invalidate drops the cached formatter instance
  PASS  Format.Number('full') renders every digit and no abbreviation
  PASS  Format.Number('full') renders a rate's DIGITS, not its decimals
  PASS  Format.Number abbreviates a sub-thousand rate to its whole part
  PASS  Format.Number renders a value BELOW ONE without dumping its float
  PASS  Format.Number('full') also covers a value below one
  PASS  Format: a formatter whose ladder never took is NOT cached
  PASS  Format: the abbreviating formatter is given breakpoints, or it does nothing
  PASS  Format.Number('') for nil, which is a different fact from zero
  PASS  Format.Number accepts a secret and returns something SetText takes
  PASS  Format.Rate renders the bare number — no unit suffix
  PASS  Format.Duration refuses the clock arithmetic on an inaccessible value
  PASS  Format.Duration does the clock arithmetic on a plain number
  PASS  Format.Percent(value, total) refuses when an operand is inaccessible
  PASS  Format.Percent computes the ratio when both operands are plain
  PASS  Format.Percent refuses a pre-computed share it cannot access
  PASS  Format.Number falls back to AbbreviateNumbers when C_StringUtil is absent
  PASS  Format.Number falls back to SafeToString when neither abbreviator exists
  PASS  Format.Rate degrades to the bare number when the suffix cannot be joined
  PASS  modules/Format.lua never calls tonumber on anything
  PASS  modules/Format.lua never calls table.concat
  PASS  Format.Number and Format.Rate contain no division at all
  PASS  The three abbreviated modes are one ladder at three decimal counts
  PASS  Each abbreviated mode caches its own formatter, so two windows cannot fight
  PASS  An unknown number format renders as the shipped one rather than raising
  PASS  A ladder the client silently refuses is DETECTED, not assumed
  PASS  Format.DeathTime renders the wall clock by default
  PASS  Format.DeathTime counts backwards from now
  PASS  Format.DeathTime answers nil when there is no timestamp at all
  PASS  Format.DeathTime never inspects a secret
  PASS  Format.DeathTime: the seconds/minutes boundary and the exact strings
  PASS  Format.DeathTime clamps a death in the future to zero
  PASS  Format.DeathTime treats any unknown style as the clock
  PASS  Format.DeathTime falls back to the clock when 'now' cannot be had
  PASS  Format.DeathTime defaults 'now' to the client clock
  PASS  Format.DeathTime routes both countdown strings through the locale
  PASS  Format.DeathTime refuses a secret timestamp in either style
  PASS  Provider: core/Compat.lua is the only file that names C_DamageMeter
  PASS  Provider: modules/Provider.lua is the only caller of the meter shims
  PASS  Provider: the source walk uses SafeIterate, never ipairs and never `#`
  PASS  Provider.IsAvailable memoizes and InvalidateAvailability drops the memo
  PASS  Provider surfaces Blizzard's failureReason verbatim
  PASS  Provider.GetColumn names an unknown stat rather than skipping it
  PASS  Provider.GetColumn tells 'no session' apart from 'session sealed'
  PASS  Provider.GetColumn copies secret fields through without inspecting one
  PASS  Provider.GetColumn skips a source row it may not access, without raising
  PASS  Provider.GetColumn drops a source with no GUID rather than keying on nil
  PASS  Provider.GetColumn preserves the API's order verbatim
  PASS  Provider never asks for Enum.DamageMeterType.Dps or .Hps
  PASS  Provider: one DamageDone read fills both halves of the Damage column
  PASS  Provider.GetSourceDetail returns Blizzard's table, guarded but unflattened
  PASS  Provider.GetSourceDetail refuses rather than handing back a sealed table
  PASS  Provider.GetSourceDetail answers nil for an unknown stat or a missing source
  PASS  Provider accepts both the dot and the colon call shape
  PASS  Provider.GetAvailableSessions passes the client's list through untouched
  PASS  Provider.GetSessionDuration hands back the opaque handle
  PASS  Provider.Reset wipes the sessions, forgets availability and announces
  PASS  Provider.Reset answers false where the API is absent
  PASS  Provider:Suspend stops the addon ASKING, not merely drawing
  PASS  Provider:Suspend drops its bus subscriptions and Resume republishes them
  PASS  Provider: with no sessionID the read goes to the TYPE shim, as it always did
  PASS  Provider: a sessionID routes to the ID shim instead
  PASS  Provider: the colon call shape carries the sessionID too
  PASS  Provider: GetSourceDetail with a sessionID reaches the ID breakdown
  PASS  Provider: a segment's duration comes off the session LIST, not the type shim
  PASS  Provider: an unknown sessionID has no duration rather than a wrong one
  PASS  Provider.HasSession is the staleness check behind a persisted segment
  PASS  Provider: a suspended capture answers no segment questions
  PASS  Provider: reading a segment never inspects a value
  PASS  Provider.ProbeSourceByGuid names what the API did with a GUID it was handed
  PASS  Provider.ProbeSourceLookup splits the verdict by whether the GUID was secret
  PASS  Provider.ProbeSourceLookup names the local player's row as the control
  PASS  Provider.ProbeSourceLookup says whether the LOCAL row's GUID was secret too
  PASS  Provider.ProbeSourceLookup answers an empty tally rather than raising
  PASS  Provider.ProbeSourceLookup withholds a SECRET creature id from the client
  PASS  Provider.ProbeSourceLookup skips a source that carries no GUID
  PASS  Provider: an NPC source with no GUID is KEPT, on its creature ID
  PASS  Provider: a source with NEITHER identifier is still dropped
  PASS  Provider: with no recap namespace the probe finds nothing, and says so
  PASS  Provider: it reports the readers the CLIENT has, not a list we wrote
  PASS  Provider: a non-function member is not a reader
  PASS  Provider: the reader list is in a stable order
  PASS  Provider: a reader that raises comes back as a refusal
  PASS  Provider: a reader that is not there comes back as a refusal too
  PASS  Provider: the value comes back unexamined
  PASS  Provider: the member dump lists EVERY member, not only recap-shaped ones
  PASS  Provider: an absent namespace is reported absent, not empty
  PASS  Provider: a reader behind a PROXY namespace is still found
  PASS  Provider: a named candidate that is not there is not called a reader
  PASS  Provider: a reader found BOTH ways is reported once
  PASS  Provider: the walk still wins the label when it can see the member
  PASS  Provider: C_DeathRecap is searched — the namespace rounds one and two missed
  PASS  Provider: GetRecapEvents is asked for BY NAME as well as walked
  PASS  Provider: GetRecapMaxHealth is searched too
  PASS  Provider.GetRecap hands back the events and the denominator together
  PASS  Provider.GetRecap is MEMOIZED — a past death never changes
  PASS  Provider.GetRecap memoizes per id, not globally
  PASS  Provider.GetRecap drops its memo when the meter is invalidated
  PASS  Provider.GetRecap answers nil when the death has no recap
  PASS  Provider.GetRecap answers nil for an id it may not use
  PASS  Provider.GetRecap never inspects an event
  PASS  Provider.GetRecap is inert while the perf harness has it suspended
  PASS  Provider.GetRecap answers a PREVIEW recap in test mode
  PASS  Provider.GetRecap in test mode does not poison the live memo
  PASS  Provider describes the RAW source row, not the projection it keeps
  PASS  The probe DESCRIBES a secret field and never carries its value
  PASS  The probe answers an empty list rather than raising on no session
  PASS  The probe is callable in both the dot and the colon shape
  PASS  The probe reports a field the addon READS and the client does not have
  PASS  The projection's field list and collectSource cannot drift apart
  PASS  The probe samples several rows, not just the first
  PASS  A field present on SOME rows only is reported as such, not as absent
  PASS  The LOCAL player's row is sampled even when it sits past the window
  PASS  Distinct PLAIN values are counted, so a constant field reads as one
  PASS  A SECRET value is never compared or keyed on to count distinctness
  PASS  Roster.GetGroup is player-first, then party order
  PASS  Roster.GetGroup carries name, class and role off the unit API
  PASS  Roster de-duplicates the player, who is both `player` and `raidN`
  PASS  Roster answers a one-entry group when solo
  PASS  Roster skips a unit the API cannot see
  PASS  Roster.IsGroupMember is a plain GUID lookup, legal at any point in a pull
  PASS  Roster.Get answers the entry, and nil for a stranger
  PASS  Roster.RoleOf answers NONE rather than nil for a non-member
  PASS  Roster normalizes an unrecognized role to NONE
  PASS  Roster.OwnerOf maps a unit-frame pet to its owner
  PASS  Roster.OwnerOf answers nil for anything that is not a unit-frame pet
  PASS  Roster never keys the map on a secret GUID
  PASS  Roster does not attribute a pet to a member it skipped
  PASS  Roster builds lazily and holds the map until it is invalidated
  PASS  Roster.Refresh drops the cache without rebuilding it eagerly
  PASS  Roster shares core/State.lua's cache seam rather than owning a private one
  PASS  Entering or leaving test mode invalidates the map
  PASS  Roster subscribes to the roster message; it never sends one
  PASS  modules/Roster.lua registers no game event of its own
  PASS  A partial build is NOT cached, so the next read retries
  PASS  A complete build IS cached
  PASS  Solo is complete, not partial
  PASS  Every member the build learns is remembered in db.global, as a plain copy
  PASS  A pet link is remembered too, and a secret one never reaches SavedVariables
  PASS  Refresh forgets the group but not the people; Forget forgets both
  PASS  The live entry is preferred over the remembered one, never the other way round
  PASS  A member whose own GUID is unreadable is left out, pet and all
  PASS  The raid duplicate is skipped WHOLE, its pet unit included
  PASS  An empty unit API yields an empty group rather than a raise
  PASS  A partial build is still STORED, so the lookups have something to answer from
  PASS  Roster.LocalGUID reads the player's GUID off the built map
  PASS  The player's role falls back to their specialization; another unit's cannot
  PASS  An assigned role beats the specialization fallback
  PASS  Test mode replaces the pet map and writes nothing to SavedVariables
  PASS  The test-mode map is cached whole, never marked partial
  PASS  Test mode with no preview group falls back to the real unit walk
  PASS  A completed build logs one line, with the counters the loop kept
  PASS  The build line says whether this was a raid
  PASS  A short build says so, and does not also claim it built the group
  PASS  Feign: the module is published and reachable
  PASS  Feign: a Feign Death cast marks that player
  PASS  Feign: any other cast marks nobody
  PASS  Feign: a SECRET spell id is not compared
  PASS  Feign: a SECRET guid is never used as a key
  PASS  Feign: a feigner confirmed at 0 HP stops being feigned
  PASS  Feign: a feigner still down stays feigned
  PASS  Feign: somebody who left the group is forgotten
  PASS  Feign: a meter reset forgets everything
  PASS  Feign: pruning an empty set costs nothing
  PASS  Feign: a death judged fake STAYS fake after the player really dies
  PASS  Feign: standing back up ends the feign, so the next death is real
  PASS  Feign: a client with no UnitIsFeignDeath keeps the old behaviour
  PASS  Feign: a reset forgets the fake deaths too
  PASS  Feign: ShouldDropDeath answers no for anything it cannot key on
  PASS  Feign: a confirmed 0 HP evicts even while the client still reads feigning
  PASS  Feign: an entry the client never confirmed is not evicted by a false reading
  PASS  Feign: a health figure that cannot be compared leaves the entry standing
  PASS  Feign: an evicted entry is traced with the state it HELD
  PASS  Feign: a surviving entry is traced with evicted=false and its raw readings
  PASS  Feign: leaving the group is traced as <not in group>
  PASS  Feign: the armed flag is read once per prune, not once per member
  PASS  Feign: a disarmed prune hands the recording nothing at all
  PASS  Feign: an absent Diagnostics file is not an error
  PASS  Feign: a group entry with no token or a secret guid counts as absent
  PASS  Feign: with no Roster at all every entry is evicted
  PASS  Feign: the set stops being walked once the last entry goes
  PASS  Aggregator joins columns on the GUID, which is the only legal key
  PASS  Aggregator's result table IS the row array, and cells aliases values
  PASS  Aggregator takes identity from the roster and the spec icon from the meter
  PASS  Aggregator promotes deathRecapID onto the row, off whichever column carried it
  PASS  Aggregator puts the column max on every cell in the column
  PASS  Aggregator publishes columnTotals so nothing re-reads the session
  PASS  Aggregator surfaces the first column reason it met
  PASS  Aggregator skips a stored column whose stat this build does not offer
  PASS  Aggregator falls back to the first column when sortColumn is unusable
  PASS  Aggregator.Build answers an empty result for a non-table window
  PASS  Aggregator.Build accepts both the dot and the colon call shape
  PASS  Aggregator drops a source that is not a group member
  PASS  Aggregator drops an unattributable pet rather than showing a phantom row
  PASS  Aggregator sums an attributed pet into its owner out of combat
  PASS  Aggregator adopts a pet's numbers into a column the owner has no cell in
  PASS  A pet's position never moves its owner in the provider order
  PASS  A row seen only outside the sort column is parked past every ranked row
  PASS  Aggregator computes percent out of combat
  PASS  ApplyRowLimit truncates to maxRows
  PASS  ApplyRowLimit treats 0 and an over-large cap as the hard ceiling
  PASS  alwaysShowSelf spends the last visible slot on the player
  PASS  alwaysShowSelf does nothing when the player is already visible
  PASS  Aggregator applies the cap before dividing, not after
  PASS  A meter reset drops this module's cache
  PASS  A pet gets its OWN row by default, with its own name
  PASS  Leaving the group does NOT empty the window
  PASS  A meter reset is what forgets them
  PASS  A pet stays attributed after its owner's group is gone
  PASS  A dropped source says WHY, once per pass
  PASS  A secret-GUID source that says it is the local player keeps its row
  PASS  A secret GUID that does NOT claim to be the local player is still dropped
  PASS  A SECRET isLocalPlayer flag is not truth-tested, and claims nothing
  PASS  A SECRET source GUID is dropped without raising, and is named as secret
  PASS  The Deaths column counts a GUID's rows rather than reading totalAmount
  PASS  A counted column scales its bars to the highest count, never to 0
  PASS  The NEWEST death wins the recap id
  PASS  An ALLY nobody owns gets its own row, under its own name
  PASS  The owner is still not credited for an unowned ally's damage
  PASS  An ENEMY nobody owns is still refused
  PASS  A DELVE COMPANION, filed under None with a real class, gets a row
  PASS  A None source with a class the CLIENT does not know is still refused
  PASS  An ENEMY with a real player class is refused, class or no class
  PASS  A source with NO display type is refused, not assumed friendly
  PASS  Every death lands in row.deaths, not just the newest
  PASS  row.deathRecapID still names the NEWEST death
  PASS  A row with no deaths carries no deaths array at all
  PASS  The count and the deaths array can never disagree
  PASS  A death the client gave no recap id still occupies a slot in the list
  PASS  A feigned player's death is not counted
  PASS  A feigned player's death is not LISTED either
  PASS  A real death after a feign is counted, and the feigns stay hidden
  PASS  A death after the hunter stands back up is counted
  PASS  The feign filter touches no column but Deaths
  PASS  A pet's DEATH lands on the row the merge put it on
  PASS  A fold the gate refuses is COUNTED, and adds nothing on the way past
  PASS  A feigned death is a SKIP, never a drop
  PASS  A counted column publishes NO column total, so its percent stays empty
  PASS  A counted column ignores the session's maxAmount, however loud
  PASS  The Deaths pass prunes the feign set itself, and no other column does
  PASS  The judge verdict is recorded per death source, after the prune
  PASS  A column that is not counted records no judgement at all
  PASS  A healer with no damage is on the mid-pull grid, from the healing column
  PASS  An ambiguous key gets no invented row, because no column could ever fill it
  PASS  A collision the LAST column reveals still blanks the FIRST column's cells
  PASS  A sort column the window does not LIST still builds mid-pull
  PASS  A correlated cell carries the RATE, or a rate column renders no text
  PASS  A correlated Deaths column keeps the recap id the death view opens on
  PASS  A pet is a ROW OF ITS OWN while restricted, not a dropped contribution
  PASS  A pet's own row survives the restriction, where a merged one would not
  PASS  Aggregator answers nil percent while restricted — never zero
  PASS  Counting a death is legal mid-pull, where summing two secrets is not
  PASS  A correlated Deaths column keeps every death too
  PASS  A collided identity key gets no deaths array, as it gets no cell
  PASS  The identity build keeps that slot too
  PASS  The feign filter cannot run mid-pull, and does not pretend to
  PASS  Identity stats count collided ROWS, not just collided keys
  PASS  Identity stats attribute every miss to one of three causes
  PASS  A collided key lands in the collided bucket, not the absent one
  PASS  The sort column is named, and is not part of the correlated rectangle
  PASS  Identity stats carry the rows-per-key histogram
  PASS  A collided key records where its sources sat in every column
  PASS  The identity stats are not built at all with the debug flag off
  PASS  The GUID build reports no identity stats, because it correlated nothing
  PASS  Aggregator keeps the last identity pass for the report to print
  PASS  Identity: the pass publishes how many ROWS wear a collided key
  PASS  Identity: an unambiguous pass publishes a count of ZERO, never nil
  PASS  A GUID pass publishes a count of zero too, because nothing was correlated
  PASS  Test mode substitutes the DATA, and the render path stays one path
  PASS  A test row's tooltip finds a breakdown, because it goes to the provider
  PASS  Test mode reaches no meter API at all
  PASS  Test data is deterministic — a jittering grid cannot be laid out against
  PASS  Test mode produces a player with several deaths to drill into
  PASS  Preview: a breakdown spell carries a RATE, not just a total
  PASS  Preview: the breakdown's rate uses the same clock as the preview grid
  PASS  Preview: a COUNTING stat's breakdown carries a rate too, and Row hides it
  PASS  value mode orders by the sort column's numbers, descending
  PASS  value mode breaks a tie on providerIndex, so the order is deterministic
  PASS  value mode sorts a row with no cell in the sort column last
  PASS  a missing cell counts as ZERO, so it leads an ascending sort
  PASS  two missing cells keep provider order, so the sort stays deterministic
  PASS  a value sort caches NOTHING — the freeze is retired
  PASS  while restricted the order is the ENGINE's ranking, and it is live
  PASS  while restricted a row is keyed on its POSITION, never on the secret GUID
  PASS  value mode checks comparability in a pass BEFORE table.sort is entered
  PASS  the Activating edge is no longer listened for
  PASS  provider mode never compares a value, in combat or out
  PASS  an unrecognized sort mode degrades to provider order rather than to nothing
  PASS  roster mode orders by group position, ignoring the numbers entirely
  PASS  roster mode ranks role before name within one group position
  PASS  roster mode's NAME TIEBREAK refuses to compare two secret names
  PASS  roster mode compares names when they are plain
  PASS  while restricted an ASCENDING sort reverses the engine's order
  PASS  an ascending mid-pull reverse leads with the rows the sort column never named
  PASS  the build PUBLISHES which order actually took effect
  PASS  `provider` mode honours the direction OUT of combat too
  PASS  Window builds a bare anchor plus the visible frame, and names both
  PASS  BuildLayout computes every coordinate from config alone
  PASS  The name column's formula is calibrated to the shipped width at a 20 cap
  PASS  A stat column never shrinks below the legible floor
  PASS  BuildLayout drops a column whose stat this build does not offer
  PASS  BuildLayout draws only the ENABLED columns, in stored order
  PASS  A layout column carries no show-bar decision
  PASS  BuildLayout derives how many rows FIT, capped by maxRows and MAX_ROWS
  PASS  The throttle is clamped to the constants, whatever the profile says
  PASS  The throttle coalesces N events into ONE refresh
  PASS  A clean window costs nothing when the clock comes round
  PASS  Refresh does nothing at all while the frame is hidden
  PASS  Refresh draws one row per aggregated entry, from the pool
  PASS  Rows come from a POOL: no CreateFrame on a second refresh
  PASS  HideAll returns every active row to the free list
  PASS  A row widget keeps its RANK across refreshes: the pool hands them back in order
  PASS  Render honors layout.maxRows and places rows from Row.OffsetFor
  PASS  growthDirection UP anchors from the bottom of the body
  PASS  R3: no geometry is read back off a cell that has held a secret
  PASS  modules/Window.lua reads geometry back off the anchor and nothing else
  PASS  An unavailable meter renders the prompt INSTEAD of rows
  PASS  The notice omits a reason it cannot safely render
  PASS  An empty session says so rather than leaving a blank grid
  PASS  A drilled-in window draws the breakdown, decided by the ROWS not the title
  PASS  Suspend takes the OnUpdate away and Resume puts it back
  PASS  Destroy takes the window off screen and off the bus
  PASS  Each window owns a PRIVATE bus target, so two windows cannot clobber each other
  PASS  Scrolling moves the window into the list, it does not shorten it
  PASS  The offset survives a refresh, or scrolling is impossible
  PASS  The offset cannot run past the end of the list
  PASS  A list that shrinks under a stationary offset re-clamps on the next draw
  PASS  Scrolling up stops at the top
  PASS  A list that fits entirely cannot be scrolled
  PASS  The body takes the wheel, or the handler is never called in game
  PASS  The wheel scrolls up on a positive delta
  PASS  Entering or leaving a breakdown puts the view back at the top
  PASS  A drill-down draws its rows from the top of the body, with none hanging out
  PASS  Right-clicking empty space below the rows leaves a breakdown
  PASS  The body claims the mouse only while a breakdown is open
  PASS  Scale scales the WINDOW, not just what is inside it
  PASS  Border style None draws NO edge, whatever the library's own is
  PASS  A border style that CANNOT be fetched still falls back to the library edge
  PASS  With no border, the SKIN's inner highlight goes too
  PASS  pool: a layout change re-applies to FREE rows, not just active ones
  PASS  Master scale MULTIPLIES the window's own rather than replacing it
  PASS  Master alpha MULTIPLIES the window's own opacity
  PASS  Master scale and alpha are CLAMPED to the sliders that write them
  PASS  The window's fill and its edge each answer a colour mode
  PASS  pool: every row built lands in `all`, including the batch surplus
  PASS  The stored per-column width is what a NEW column is born at, never the drawn one
  PASS  The name column is excluded from the share, and is not sized by the frame
  PASS  The name column is placed first, at x 0, and carries its bar unconditionally
  PASS  A window with every column disabled still lays out
  PASS  Hiding the title bar takes its height out of the layout, not out of the header strip
  PASS  bodyWidth is the frame minus its padding, whatever the grid inside it costs
  PASS  growUp is a boolean off growthDirection, and nothing else
  PASS  A window too short for even one row still asks the pool for one
  PASS  A maxRows cap LARGER than the frame holds does not win
  PASS  BuildLayout survives a config with the sub-tables missing, on the shipped numbers
  PASS  The header carries a lock and a gear, and the padlock shows the state
  PASS  The padlock toggles THIS window only
  PASS  The title-bar divider can be switched off, and does not move the title row
  PASS  The divider's thickness is a setting
  PASS  The divider's SKIN mode writes no colour at all, so a re-skin still reaches it
  PASS  The divider takes a custom colour and a class colour, keeping the configured alpha
  PASS  The header folds its parts with `..`, and survives a secret piece
  PASS  The header line says which fight, and stays out of the way otherwise
  PASS  Show segment off leaves the header line blank again
  PASS  The segment name sits LAST, nearest the picker that changes it
  PASS  The restricted notice COUNTS the rows it could not tell apart — #22
  PASS  The restricted notice stays a bare word when NOTHING collided
  PASS  The header says the grid was built the restricted way
  PASS  The header names AMBIGUITY when two rows cannot be told apart
  PASS  The header line reads 'Test' while placeholder data is on screen
  PASS  Test data never reaches the provider
  PASS  UNLOCKING A WINDOW NO LONGER TURNS TEST DATA ON
  PASS  Segment menu: stored segments first, then a divider, then Current/Overall
  PASS  Segment menu: an entry is labelled with its name AND its duration
  PASS  Segment menu: picking a segment pins it and marks the window dirty
  PASS  Segment menu: picking Current CLEARS the pin
  PASS  Segment menu: with no menu API the click is refused, not an error
  PASS  Segment: a pinned segment is READ, not merely stored
  PASS  Segment: the header names the pinned segment rather than lying `Current`
  PASS  Segment: a stale pin is dropped on the next refresh
  PASS  Segment: a LIVE pin survives the staleness check
  PASS  Segment: with no provider the pin is left alone rather than rewritten
  PASS  Segment: the session line takes NO mouse
  PASS  Column headers are BUTTONS carrying the full stat label, left-aligned
  PASS  The sort column shows an arrow and the others do not
  PASS  The arrow flips with the direction
  PASS  The sort arrow prefers the collection's own art over the Blizzard atlas
  PASS  The sort arrow's two directions are two assets, never one flipped
  PASS  The sort arrow falls to the Blizzard atlas with no LibKa0s art
  PASS  Clicking a header sorts by it; clicking again reverses
  PASS  Clicking a header drops the frozen sort order
  PASS  A STAT header is honoured in combat: the column it ranks by is a choice
  PASS  A stat header REVERSES in combat too, because reversing compares nothing
  PASS  The Player header sorts by PLAYER, ascending first
  PASS  The Player header is the ONE that still refuses while restricted
  PASS  Mid-pull the arrow sits on the column the rows are ACTUALLY ordered by
  PASS  The sort arrow moves to the Player header in name mode
  PASS  Test mode is marked in RED in the title, and clears when it is off
  PASS  Building a window sets no text on a fontless FontString
  PASS  Header art falls back to ASCII on a client with none of the atlases
  PASS  Header art prefers an atlas where the client has one
  PASS  Column headers take their own font, not the cells'
  PASS  Column headers have their own colour and background
  PASS  Per-statistic mode leaves the Player header white, not the sort column's colour
  PASS  The Player header's sort arrow is white too, in per-statistic mode
  PASS  Minimise hides everything below the title bar
  PASS  Minimise actually shrinks the window
  PASS  Minimise leaves the STORED height alone, so expanding restores it
  PASS  A collapsed window does not aggregate or render
  PASS  A collapsed window keeps the notice hidden
  PASS  Header shadow reaches every line of the strip
  PASS  Column header shadow is its OWN setting, not the header's
  PASS  The header's text answers TWO modes, and the custom one is the picker
  PASS  The header's text takes the CLASS colour, keeping the configured alpha
  PASS  The header's text mode offers class and custom, and NOT per-statistic
  PASS  The header colour survives a sort change, having nothing to do with it
  PASS  The window NAME takes the header's colour
  PASS  Column header class color is the local player's too
  PASS  A profile written before minimise existed is not collapsed
  PASS  Header buttons are created ONCE per index and re-pointed, never rebuilt
  PASS  A column that goes away HIDES its header; it does not destroy it
  PASS  Every header sits exactly over the column it labels, from the same layout
  PASS  The Player header is a Button like every other, not a label with a gap beside it
  PASS  The strip background and the per-column ones are mutually exclusive, both ways
  PASS  The sort arrow follows the LABEL, rather than sitting at a fixed offset
  PASS  The atlas rung flips ONE texture with SetTexCoord, and only for ascending
  PASS  With no art and no atlas the arrow is an ASCII character, and a legible one
  PASS  Closing HIDES the window; it never deletes it
  PASS  Dragging moves the ANCHOR, never the frame that holds the cells
  PASS  A locked window refuses the drag entirely
  PASS  SavePosition reads GetPoint off the anchor and off nothing else
  PASS  SaveSize uses the size OnSizeChanged was handed, never a getter
  PASS  The window refuses to be dragged smaller than the grid needs
  PASS  ShouldShow STEP 0 is NS.Perf.suspended, above even the master enable
  PASS  ShouldShow's ladder reads master enable, then test mode, then context
  PASS  RefreshVisibility shows, hides, and marks dirty exactly once on the way in
  PASS  An explicit Show draws on the next tick too, not a throttle later
  PASS  Leaving test mode does not close the window
  PASS  A player-state edge re-runs the show ladder on the window itself
  PASS  A combat edge re-runs the show ladder on the window itself
  PASS  The show ladder is re-run ONLY from a message, never from the refresh tick
  PASS  Entering a vehicle hides the window on its own edge
  PASS  A vehicle event about somebody else is not republished
  PASS  A state that lags its own event is caught by the settle pass
  PASS  The settle pass is scheduled once, however many edges land together
  PASS  A glide event's boolean payload is not mistaken for a unit token
  PASS  A filtered-out unit event schedules nothing
  PASS  Changing a setting does not close a window the player asked for
  PASS  The master switch closes a window the player asked for
  PASS  A perf suspend closes a window the player asked for
  PASS  A CONTEXT rule still cannot close a window the player asked for
  PASS  A zone change is what makes an explicit show stale
  PASS  Closing cancels the request, so it does not reappear
  PASS  The resize grip is built unconditionally and follows the LOCK
  PASS  `resizeGrip` is gone from the code, not just from the panel
  PASS  Unlocking does not resurrect the grip on a collapsed window
  PASS  Either lock pins the window, and the master lock erases neither
  PASS  HeaderControls: every control this addon builds is attached
  PASS  HeaderControls: a control turned off is not placed at all
  PASS  HeaderControls: a hidden control YIELDS its slot
  PASS  HeaderControls: hiding the LAST control moves nothing
  PASS  HeaderControls: the strip is CENTRED in the title bar
  PASS  HeaderControls: the strip and the title share one centre line
  PASS  HeaderControls: a control taller than its bar overflows DOWNWARD
  PASS  HeaderControls: a control at rest takes the control colour
  PASS  HeaderControls: the control under the pointer takes the HOVER colour
  PASS  HeaderControls: both colours come from config
  PASS  HeaderControls: each colour has its OWN colour mode
  PASS  HeaderControls: the hover mode classes the hover colour and nothing else
  PASS  HeaderControls: both flags off is the shipped look, unchanged
  PASS  HeaderControls: control size comes from config
  PASS  HeaderControls: the width reserved equals the width occupied
  PASS  HeaderControls: no title bar means no strip and no reservation
  PASS  HeaderControls: with no atlas the ASCII rung draws
  PASS  HeaderControls: an atlas beats the ASCII rung
  PASS  HeaderControls: an unlocked padlock is drawn at the same weight as its neighbours
  PASS  HeaderControls: the padlock's two states do not draw the same
  PASS  HeaderControls: minimise shows the opposite of the state it is in
  PASS  HeaderControls: a glyph is never given text before a font
  PASS  HeaderControls: reset asks before it wipes anything
  PASS  HeaderControls: the reset confirmation opens in the CENTRE of the screen
  PASS  HeaderControls: minimise writes through the settings seam
  PASS  HeaderControls: the lock button toggles this window only
  PASS  HeaderControls: only the control under the pointer is revealed
  PASS  HeaderControls: the title bar itself reveals nothing
  PASS  HeaderControls: the reveal moves rather than accumulating
  PASS  HeaderControls: with the reveal off, hover is colour alone
  PASS  HeaderControls: both ends of the reveal are settings, and default to what was hardcoded
  PASS  HeaderControls: the two opacity sliders each move their own end
  PASS  HeaderControls: with the reveal off every control sits at the HOVER opacity
  PASS  HeaderControls: an out-of-range opacity is clamped, not passed through
  PASS  HeaderControls: hover reveal off means always visible
  PASS  HeaderControls: hooking hover does not unseat the drag
  PASS  HeaderControls: a locked window can still reveal its controls
  PASS  HeaderControls: our own art is the FIRST rung
  PASS  HeaderControls: a missing TGA falls through to the atlas
  PASS  HeaderControls: a failed path is not left set under the next rung
  PASS  HeaderControls: a click writes to the window it was clicked ON
  PASS  HeaderControls: the gear points the panel at its own window
  PASS  HeaderControls: the padlock's ASCII rung differs between states
  PASS  HeaderControls: the strip fits at every size the schema allows
  PASS  HeaderControls: a degraded install still gets its close button
  PASS  HeaderControls: the close button closes the window
  PASS  HeaderControls: the art is drawn INSIDE its slot, not across it
  PASS  HeaderControls: a control keeps its reveal as the pointer arrives
  PASS  HeaderControls: the segment button opens the same menu the session line does
  PASS  HeaderControls: the export button hands Export the WINDOW
  PASS  HeaderControls: the gear opens the panel
  PASS  HeaderControls: a click with no window, or no control, does nothing
  PASS  HeaderControls: a control name the chain does not know is a silent no-op
  PASS  HeaderControls: the two toggles name the EXACT settings paths
  PASS  HeaderControls: a toggle inverts what is STORED, as a boolean
  PASS  HeaderControls: a toggle points the seam at this window BEFORE it writes
  PASS  HeaderControls: with no settings seam a toggle changes nothing and raises nothing
  PASS  HeaderControls: the gear sets the active window BEFORE it opens the panel
  PASS  HeaderControls: the gear still points the panel when there is no panel
  PASS  HeaderControls: reset PREFERS the centred dialog over the bare popup
  PASS  HeaderControls: with settings/ absent the reset falls back to the bare popup
  PASS  HeaderControls: reset with no popup API at all raises nothing
  PASS  HeaderControls: segment and export are opened ON the window, or not at all
  PASS  HeaderControls: close hides the window AS a deliberate close
  PASS  HeaderControls: only the two toggles write to the settings seam
  PASS  Row.OffsetFor is a pure function of the index and the row config
  PASS  Cell:ApplyLayout places every cell from the layout table
  PASS  A column toggled off hides its cell rather than destroying it
  PASS  modules/Row.lua contains no geometry getter at all
  PASS  Cell:SetValue hands the raw handle to SetValue and SetMinMaxValues
  PASS  Cell:SetValue substitutes 0 and 1 for an ABSENT figure, not for a hidden one
  PASS  A rate-capable column renders its RATE ALONE by default
  PASS  Smart falls to the ABSOLUTE figure on a stat that has no rate
  PASS  Combined shows BOTH figures in one slot, absolute first
  PASS  Combined falls to the absolute ALONE on a stat with no rate
  PASS  Both figures appear when the right slot is turned on
  PASS  A counting column renders the total only
  PASS  The text slots are configurable, and percent is the one that goes quiet
  PASS  A 'none' text slot renders nothing
  PASS  A cell with BOTH slots off shows NOTHING — none means none
  PASS  A counting stat set to Per second renders nothing, not its total
  PASS  A lone RIGHT-slot figure stays on the right
  PASS  Both slots take the same six values, in either position
  PASS  Cell figures are read out of EITHER row shape the addon produces
  PASS  Class color comes from classFilename, which keeps working while restricted
  PASS  Every color mode falls back to one neutral, never to a fourth palette
  PASS  Role and stat color modes read their own tables
  PASS  A custom color comes from the setting, not from the last-resort literal
  PASS  Every cell draws its bar, because the bar is not optional
  PASS  Cell text takes the ROW's class color when asked
  PASS  Class color keeps the configured ALPHA, not the class's own
  PASS  With no class to read, cell text keeps its configured color
  PASS  Text opacity fades the TEXT, and leaves the bar alone
  PASS  The bar border takes the player's thickness and colour
  PASS  The bar border answers a colour mode, and its class is the ROW'S player
  PASS  The bar border's shipped mode is Custom, so an upgraded window looks the same
  PASS  The bar border is drawn ABOVE the fill, not under it
  PASS  Border style None keeps the cheap flat outline and puts no backdrop on a cell
  PASS  Border style art moves to a backdrop and takes the flat outline down
  PASS  A window that sets neither keeps the border it always had
  PASS  The border setting off builds no texture at all
  PASS  Turning the border off hides all four sides and keeps the textures
  PASS  Each of the four sides is anchored to its own two corners
  PASS  A second layout pass re-places the border rather than stacking anchors
  PASS  A thickness under one pixel is clamped to one, on both paths
  PASS  The art path takes the edge FILE and the swatch's colour
  PASS  The art path answers the colour mode too, and it is the ROW'S class
  PASS  Switching from the flat outline to art takes ALL FOUR sides down
  PASS  Per-statistic cell text is the colour of the column the cell is in
  PASS  The statistic palette is a SETTING, and every surface reads it through one seam
  PASS  A statistic the profile has never coloured keeps the shipped palette
  PASS  Text opacity reaches the NAME and the numbers alike, class colour or not
  PASS  Text opacity and the colour's own alpha multiply, rather than one winning
  PASS  Bar opacity fades the FILL, and nothing else in the cell
  PASS  Bar opacity and text opacity are independent, in both directions
  PASS  highlightSelf honors both spellings of 'this row is you'
  PASS  The class tint is painted on the CELLS, not on the row
  PASS  A row with no class falls back to the alternating stripe
  PASS  The class tint can be switched off
  PASS  The mouseover overlay is driven from the CELLS, and honors the setting
  PASS  On the grid the mouse goes to every cell, including the name cell
  PASS  Release blanks the row without destroying a widget
  PASS  Hovering a stat cell asks the tooltip the narrow question
  PASS  Clicking a stat cell routes to the drill-down; the name cell does not
  PASS  A cell with no entry does nothing under the cursor
  PASS  Hovering a breakdown ROW shows the client's spell tooltip
  PASS  A breakdown row takes the mouse, and its cells give theirs up
  PASS  Hovering a breakdown row lights its highlight, since no cell can
  PASS  Crossing a cell boundary does NOT blink the breakdown tooltip
  PASS  Leaving the row hides the breakdown tooltip
  PASS  On the GRID a cell still owns its own tooltip
  PASS  A breakdown row with no resolvable spell still says which spell it is
  PASS  A left click inside a breakdown does nothing
  PASS  A right click leaves the breakdown
  PASS  A right click on the ROW ITSELF leaves the breakdown
  PASS  A right click on the GRID is a harmless no-op
  PASS  Cells register for BOTH buttons, or the right click never arrives
  PASS  Row: a cell renders displayText in place of its number
  PASS  Row: displayText wins over BOTH slots
  PASS  Row: a cell with no displayText is completely unaffected
  PASS  Row: a death row's bar draws FULL without comparing anything
  PASS  The name cell is never handed a meter value at all
  PASS  The name cell colors the NAME by class, now that no bar carries it
  PASS  An unknown class reads as white, not as a tenth palette entry
  PASS  The name cell renders a plain name and survives a secret one
  PASS  A cross-realm PLAYER name loses its realm
  PASS  An NPC keeps the hyphen in its name
  PASS  A pet keeps its hyphen too
  PASS  The name never wraps, and gets a fixed width to be truncated against
  PASS  The icon inset is the SAME for a row with no icons to draw
  PASS  A layout pass keeps the class color instead of flashing white
  PASS  A name past the cap is truncated with NO ellipsis
  PASS  Truncation counts CHARACTERS, never bytes
  PASS  A cap of 0 means no cap
  PASS  Neither the realm strip nor the cap is applied to a SECRET name
  PASS  A drill-down row keeps a hyphen, which is part of a spell name
  PASS  A nil name renders empty rather than the string 'nil'
  PASS  The single icon slot prefers the SPEC where there is one
  PASS  The slot falls back to the CLASS where no spec is known
  PASS  A ROLE icon is never drawn, whatever the row carries
  PASS  A breakdown row draws the SPELL's icon, not a unit's
  PASS  Turning the icon off hides it rather than destroying it
  PASS  An icon turned off STAYS off across the next refresh
  PASS  The name starts clear of the icon, with a gap you can see
  PASS  Icons on the RIGHT anchor to the right edge and give the name the left one
  PASS  The icon takes its configured size and is centred in the row
  PASS  ApplyIcons returns the inset it consumed, and never zero
  PASS  The slot list and the drawn flag are what SetPlayer reads
  PASS  A narrow name column still leaves the string a width of at least one
  PASS  A window config with no icons group at all draws a name and does not raise
  PASS  Re-laying the icons out does not stack anchors on the texture or the name
  PASS  Targets: a player's enemies are recovered from the enemy column
  PASS  Targets: one enemy's several spells are summed into one line
  PASS  Targets: another player's damage is not credited to this one
  PASS  Targets: a player who hit nothing gets nil, not an empty list
  PASS  Targets: the cap trims the list after ordering, not before
  PASS  Targets: Total adds a list up, for the share column
  PASS  Targets: an unreadable amount abandons the WHOLE build, not one enemy
  PASS  Targets: the enemy lookup drops a SECRET guid and resolves on creatureID
  PASS  Targets: a SECRET creatureID is dropped rather than handed to the API
  PASS  Targets: with neither identifier readable the section is ABANDONED, not guessed
  PASS  Targets: an unreadable caster name is skipped, and skipping it is safe
  PASS  Targets: an unreadable HOVERED name answers nil rather than raising
  PASS  Targets: no enemy column means no section, not an error
  PASS  Targets: every meter read goes through the provider
  PASS  Targets: a cross-realm caster still matches the row it belongs to
  PASS  Targets: a realm-qualified ROW name matches a bare caster
  PASS  Targets: two casters differing only by realm are still told apart by name
  PASS  Targets: one walk answers for every player, not just the hovered one
  PASS  Targets: a second hover of the same player reads nothing
  PASS  Targets: the cap is applied to a COPY, never to the cached list
  PASS  Targets: a refusal does not pin the section shut for the session
  PASS  Targets: a new session's numbers replace the old ones
  PASS  Targets: the invalidating messages are actually subscribed
  PASS  Targets: two sessions do not share a map
  PASS  modules/Tooltip.lua never applies `#` to a meter array
  PASS  Every anchor the schema offers resolves to a real GameTooltip token
  PASS  Each anchor puts the tooltip in the box of a 3x3 around the cell
  PASS  There is no "At cursor" anchor, and TOP is what shipped instead
  PASS  The anchor dropdown offers nothing the token table cannot resolve
  PASS  The x/y offset reaches SetOwner rather than a SetPoint of our own
  PASS  A junk offset off an old profile is clamped, never handed to the client
  PASS  Bar spacing is applied to the tooltip, and taken back off when it hides
  PASS  The configured font reaches both number slots and the spell name
  PASS  NONE is an absent outline flag, not the literal string
  PASS  Every tooltip line we restyled is put back when the tooltip hides
  PASS  maxSpells 0 lists every spell the breakdown collected
  PASS  maxSpells 0 is bounded by the collector, and says so
  PASS  A negative or non-numeric cap still falls back to the shipped default
  PASS  The font survives a UI skin that re-fonts every line on show
  PASS  The post-layout pass still restores every line it touched
  PASS  A target's name is drawn on our own carrier, not on the tooltip's line
  PASS  The gap above a section is half the text size, not a whole blank line
  PASS  The half-size gap survives the post-layout pass
  PASS  The gap is restored with every other line it was applied alongside
  PASS  A spell line carries a real class-colored BAR, not a run of characters
  PASS  Bars are released between hovers, never stacked
  PASS  The bar is DRAWN mid-pull, because the widget does the division
  PASS  A bar spans the FULL line, so its length is comparable down the column
  PASS  A bar clears the icon rather than running underneath it
  PASS  The player's name is class-coloured on every tooltip that names one
  PASS  A row with no class keeps a white name rather than an invented colour
  PASS  The scale reaches the tooltip BEFORE it is placed, and is put back after
  PASS  A nonsense scale is bounded rather than handed to the client
  PASS  The bar's fill and its backdrop each take their own colour and opacity
  PASS  Per-statistic mode is the HOVERED column's colour, not the sort column's
  PASS  The text mode follows the hovered column too
  PASS  Class mode paints the bar with the hovered player's class
  PASS  The bar border is drawn on the BAR, where it can be seen
  PASS  The bar border answers a colour mode, and its class is the HOVERED player's
  PASS  The bar border's shipped mode is Custom, so it still reads the swatch
  PASS  A bar sits UNDER the tooltip's text, not over it
  PASS  Bars come down when GameTooltip closes, whoever closed it
  PASS  A spell line carries its SHARE of the player's total beside the amount
  PASS  The percent slot GOES QUIET mid-pull rather than approximating
  PASS  The amount and the share sit in FIXED right-aligned slots
  PASS  Both number slots are white by default, not two kinds of number
  PASS  The tooltip text colour is configurable, and reaches every slot
  PASS  The AMOUNT rides on the carrier, not on the bar
  PASS  The tooltip is widened for the slots, and put back afterwards
  PASS  The tooltip's own bar texture is used, not the grid's
  PASS  Tooltip text takes the HOVERED player's class color when asked
  PASS  With the class colour off, the tooltip keeps its configured text colour
  PASS  Tooltip shadow reaches the line, and survives the post-Show re-font
  PASS  The tooltip puts a SHARED line's shadow back when it lets go
  PASS  A bar border is applied when asked and cleared off the POOLED line when not
  PASS  Border size zero drops the border FILE with it
  PASS  The tooltip is widened without measuring anything inside GameTooltip
  PASS  The width follows the font size, because it is computed
  PASS  A name that cannot be read does not change the width, because none is read
  PASS  The name's room is a FIXED span, not the length of the names on screen
  PASS  The share slot fits a full 100.0%, at any configured font size
  PASS  CellTooltip opens on the hovered cell and heads with the player and the stat
  PASS  CellTooltip honors the anchor setting and falls back to the default
  PASS  CellTooltip sorts biggest-first when comparison is legal
  PASS  CellTooltip REFUSES the sort while comparison is illegal
  PASS  CellTooltip refuses the sort when an amount is MISSING, not merely secret
  PASS  CellTooltip caps the list at maxSpells and says how many were left out
  PASS  CellTooltip renders secret amounts through the formatter, untouched
  PASS  CellTooltip says 'no data' rather than showing an empty frame
  PASS  showSpells = false keeps the header and drops the breakdown
  PASS  hideInCombat refuses the hover outright
  PASS  An unresolvable spell is shown by ID rather than dropped
  PASS  The avoidable column tags nothing per spell — no Deadly, no Overkill
  PASS  Those flags are never truth-tested anywhere — a secret boolean would raise
  PASS  The Deaths cell advertises the click that opens the recap
  PASS  A death with no recap id advertises nothing
  PASS  NameTooltip lists EVERY tracked stat, dimming the ones not on screen
  PASS  NameTooltip colors each stat by the catalog palette, whatever colorMode says
  PASS  NameTooltip colors the AMOUNT the same as its label, on both sides of the line
  PASS  NameTooltip works while restricted, adding nothing up
  PASS  showAllStatsOnName = false stops after the name
  PASS  NameTooltip says 'no data' when the meter has nothing for the player
  PASS  A tooltip resolves its window from row.windowId when it was not handed one
  PASS  Tooltip:Hide is unconditional
  PASS  Tooltip: hovering a death row lists its events, OLDEST first
  PASS  Tooltip: a death row never reaches the client's spell tooltip
  PASS  Tooltip: each event line shows the time before death and the attacker
  PASS  Tooltip: an event with no attacker renders without the clause
  PASS  Tooltip: the bar behind an event is HP REMAINING, handed over raw
  PASS  Tooltip: the killing blow is the LAST line, and carries no overkill
  PASS  Tooltip: a secret spell name never meets concatenation
  PASS  Tooltip: with the values secret the bar still draws and the share does not lie
  PASS  Tooltip: with the values plain the share slot carries the percentage
  PASS  Tooltip: a death whose recap has gone says so instead of drawing nothing
  PASS  Tooltip: a death with no recap id says so rather than showing a name
  PASS  Tooltip: a death tooltip has a HEADER, so no carrier ever lands on line 1
  PASS  Tooltip: the death header names the player and when they died
  PASS  Tooltip: a death with no recap keeps the header too
  PASS  Tooltip: a Deaths cell lists the DEATHS, not a spell breakdown
  PASS  Tooltip: a Deaths cell shows one line per death, newest first
  PASS  Tooltip: the death line ships naming the KILLER and not the spell
  PASS  Tooltip: a death line names who and what landed the killing blow
  PASS  Tooltip: either half of a death line can be switched off on its own
  PASS  Tooltip: a death with nothing to name is still a numbered death
  PASS  Tooltip: a melee killing blow is named Melee rather than left blank
  PASS  Tooltip: a secret caster or spell name is left off rather than joined
  PASS  Tooltip: a Deaths cell still says a click opens the list
  PASS  Tooltip: a cell for a player who has NOT died is unchanged
  PASS  Tooltip: the death breakdown gets the same gap and caption every section has
  PASS  Tooltip: an event's time, spell and caster are three separate slots
  PASS  Tooltip: each slot has a reserved width, so a long name cannot push the numbers off
  PASS  Tooltip: the amount slot carries the damage ALONE
  PASS  Tooltip: a spell line gets its slots BACK after an event line used them
  PASS  Tooltip: an event with no caster leaves that column empty, not absent
  PASS  Tooltip: the death tooltip reserves a WIDER minimum than a spell breakdown
  PASS  Tooltip: a Deaths cell's rows carry a FULL bar, not an empty one
  PASS  Tooltip: every death line in a Deaths cell carries the skull icon
  PASS  Tooltip: a Deaths cell's time sits at the right edge, where a share does
  PASS  Tooltip: the caster column's CAP is narrower than the spell column's
  PASS  Tooltip: both name columns refuse to wrap, so the engine clips them
  PASS  Tooltip: a melee swing reads as Melee, not as #?
  PASS  Tooltip: a heal with no spell id reads as Heal
  PASS  Tooltip: an event with an id the client cannot name still shows the id
  PASS  Tooltip: a SECRET event type is not compared
  PASS  Tooltip: the time column is sized to the widest time in THIS recap
  PASS  Tooltip: the name columns shrink to the names actually in this recap
  PASS  Tooltip: a name column never grows past its character cap
  PASS  Tooltip: a SECRET name falls back to the fixed reservation
  PASS  Tooltip: an event with no id and no name reads as #? under the question mark
  PASS  Tooltip: a DIRECT heal with no spell id reads as Heal too
  PASS  Tooltip: an event AFTER the moment of death keeps the sign OFF
  PASS  Tooltip: a refused offset empties the time slot and reserves the column
  PASS  Tooltip: a recap whose events are not an array says so and draws nothing
  PASS  Tooltip: an EMPTY event array is a refusal, not an empty list
  PASS  Tooltip: a SECRET event array is refused whole, never indexed
  PASS  Tooltip: an entry that is not a table is skipped, and the rest still draw
  PASS  Tooltip: the collect stops at 64 events, keeping the NEWEST of them
  PASS  Tooltip: ONE unreadable caption abandons the whole measurement
  PASS  modules/Tooltip.lua never applies `#` to a recap's event array
  PASS  Tooltip: a stat key the catalog does not know heads with the key itself
  PASS  Tooltip: a Deaths cell reads deathTimeFormat off the WINDOW's text block
  PASS  DrillDown.IsActive is a PLAIN BOOLEAN, in both directions
  PASS  Enter captures PLAIN identity fields, never a reference to the row
  PASS  Enter refuses a row with no GUID and a stat this build does not offer
  PASS  Enter and Exit announce on the bus, with the window id and a boolean
  PASS  Exit is a no-op when nothing is open
  PASS  Drill-down state is session-only, in the shared cache
  PASS  BuildRows returns nil, nil and a FALSE BOOLEAN when the window is not drilled in
  PASS  BuildRows emits aggregator-shaped rows, one per spell
  PASS  BuildRows does NOT sort — the API's order is kept exactly
  PASS  BuildRows builds while restricted, comparing and totaling nothing
  PASS  BuildRows skips a spell row it may not access
  PASS  BuildRows caps the breakdown at the row pool's ceiling
  PASS  BuildRows answers an empty list, not nil, when the provider refuses
  PASS  An unresolvable spell keeps its synthesized key as its name
  PASS  DrillDown.Title is text for a widget, and nil when nothing is open
  PASS  A view with no name still produces a title, without an `or` on the name
  PASS  The window branches on BuildRows' rows, never on its title
  PASS  Clicking a stat cell enters the breakdown
  PASS  Clicking the same cell again returns to the grid
  PASS  Clicking a DIFFERENT cell while drilled in switches the view
  PASS  The Deaths cell routes to the death recap via deathRecapID
  PASS  A Deaths click falls through to the breakdown when the recap API is absent
  PASS  A Deaths click with no recap id falls through too
  PASS  A Deaths click prefers the Compat shim the moment one exists
  PASS  A click on something that is not a row does nothing
  PASS  The back button is created once and re-used forever after
  PASS  The back button is anchored, never measured
  PASS  The back button exits the drill-down
  PASS  A meter reset leaves every drill-down
  PASS  Deleting a window leaves the drill-down that belonged to it
  PASS  A bulk registry change sweeps views whose window is gone
  PASS  A Deaths click on a player who died enters the DEATHS view
  PASS  The deaths view lists one row per death, newest first
  PASS  A death row is numbered CHRONOLOGICALLY, so the list counts down
  PASS  A death row carries the wall-clock time as its cell caption
  PASS  A death whose recap the client has dropped still gets a row
  PASS  A Deaths click on a player with no deaths array keeps today's behaviour
  PASS  With no C_DeathRecap a Deaths click does not enter an empty deaths view
  PASS  A second click on the same Deaths cell leaves the deaths view
  PASS  The deaths view never asks the provider for a spell breakdown
  PASS  A death row is flagged as a drill-down row
  PASS  The deaths view is capped like every other breakdown
  PASS  Clicking a death row opens the game's own recap window
  PASS  Clicking a death row does not leave the list
  PASS  Right-clicking a death row still leaves the list
  PASS  Clicking a SPELL row is still a no-op
  PASS  Clicking a death whose recap has gone does nothing rather than opening an empty frame
  PASS  A death with no recap id is still a row, and is not clickable
  PASS  Every death row has a distinct pool identity, id or no id
  PASS  A death row wears the death icon
  PASS  The exit toggle keys on the GUID too, not the stat alone
  PASS  A row that is not a table is refused whatever it is
  PASS  A row with no guid is refused, and the window stays on the grid
  PASS  A stat key outside the catalog is refused
  PASS  The Deaths ladder falls all the way to the ordinary breakdown
  PASS  A client with no C_DeathRecap and no id still reaches the breakdown
  PASS  The exit toggle is answered BEFORE the Deaths ladder is climbed
  PASS  Switching out of a deaths view replaces the state, it does not merge into it
  PASS  Export is a plain table on NS, not an AceAddon module
  PASS  Export.Available says yes out of combat, with nothing to explain
  PASS  Export.Available refuses while the Combat restriction is active
  PASS  Export.Available resolves Secrets at CALL time, not at load
  PASS  Export.HeaderName turns a catalog key into snake_case
  PASS  Export.HeaderName covers every key in the catalog and invents no gaps
  PASS  Export.CsvField leaves an ordinary field alone
  PASS  Export.CsvField quotes a field carrying a comma
  PASS  Export.CsvField doubles an embedded double quote and wraps the field
  PASS  Export.CsvField quotes a field carrying a CR or an LF
  PASS  Export.CsvField leaves the two names from the issue unquoted
  PASS  Export.CsvField answers empty for nil, never the string 'nil'
  PASS  Export.CsvField answers empty for a value it may not look at
  PASS  Export.Columns carries _ps for exactly the rate stats and no others
  PASS  Export.Columns carries a total and a _pct for every stat in the catalog
  PASS  Export.Columns follows catalog order and states each fact twice
  PASS  Export.SessionConfig names every catalog stat, enabled
  PASS  Export.SessionConfig caps at the aggregator's own ceiling
  PASS  Export.SessionConfig inherits the invoking window's segment
  PASS  Export.SessionConfig takes pet merging from the PROFILE, not the window
  PASS  Export.SessionConfig falls back to the Current session with no window
  PASS  Export.SessionConfig takes a Window INSTANCE as well as a bare config
  PASS  Export.SessionConfig ranks by the requested column, then the window's
  PASS  Export.CSV leads with the identity headers, then the derived stat columns
  PASS  Export.CSV writes one row per entry and terminates with a CRLF
  PASS  Export.CSV puts the session and duration on EVERY row
  PASS  Export.CSV rounds a fractional duration to whole seconds
  PASS  Export.CSV leaves the duration blank rather than reading a secret one
  PASS  Export.CSV writes raw values and a two-decimal share
  PASS  Export.CSV writes an absent cell as blank, never as a zero or a 'nil'
  PASS  Export.CSV quotes a session name that would otherwise split the row
  PASS  Export.CSV writes a header and nothing else for an empty result
  PASS  Export.CSV reads the degenerate build's separate rows array
  PASS  Export.CSV answers nothing at all for something that is not a result
  PASS  Export.CSV refuses while the Combat restriction is active
  PASS  Export.ChatLines heads the dump with the addon, the metric and the segment
  PASS  Export.ChatLines omits the segment from the header when there is none
  PASS  Export.ChatLines numbers the rows and never reorders them
  PASS  Export.ChatLines abbreviates the amount and carries rate then share
  PASS  Export.ChatLines drops the rate parenthetical for a counted stat
  PASS  Export.ChatLines drops the share when the aggregator could not compute one
  PASS  Export.ChatLines names an unreadable row rather than dropping it
  PASS  Export.ChatLines prints five ranked lines by default
  PASS  Export.ChatLines clamps the cap to 1..MAX_ROWS
  PASS  Export.ChatLines falls back to the first catalog stat for an unknown key
  PASS  Export.ChatLines answers nothing rather than raising with no formatter
  PASS  Export.ChatLines refuses to the empty array while restricted
  PASS  Export.ChatLines refuses anything that is not a result table
  PASS  Export.ChatLines joins an empty segment name rather than asking whether it is empty
  PASS  Export.ChatLines omits a segment name that is not a string
  PASS  Export.ChatLines takes the duration from the result and never from the formatter
  PASS  Export.ChatLines asks the formatter for the amount, the rate and the share by name
  PASS  Export.ChatLines carries the rate alone when the aggregator computed no share
  PASS  Export.ChatLines drops a share the aggregator did not answer as a number
  PASS  Export.ChatLines prints a zero share rather than reading it as absent
  PASS  Export.ChatLines names a row that has no cell for the metric anyway
  PASS  Export.ChatLines reads the rows off result.rows when it is not the result itself
  PASS  Export.ChatLines answers a header and nothing under it for a result with no rows
  PASS  Export.ResolveChannel answers nothing for SELF, which is the default
  PASS  Export.ResolveChannel folds the RETIRED "AUTO" key to self only
  PASS  The channel catalog no longer offers AUTO
  PASS  Export.ResolveChannel trims a whisper target and refuses an empty one
  PASS  The TARGET channel whispers whoever the player has targeted
  PASS  A cross-realm target keeps its realm, because a whisper needs it
  PASS  TargetName refuses no target and a target that is not a player, separately
  PASS  TARGET with nothing targeted resolves to SELF rather than to a broken send
  PASS  Export.ResolveChannel takes every other key from the catalog
  PASS  Export.ResolveChannel treats an unknown key as its own chat type
  PASS  Export.Send prints to the player alone for SELF
  PASS  Export.Send hands one message per line to the client
  PASS  Export.Send sends every line at once on a client with no C_Timer
  PASS  Export.Send prints locally for SELF and sends nothing to any channel
  PASS  Export.SendDelay takes an extra second every batch, to duck the message counter
  PASS  Export.NeedsHardwareEvent is true for Say outdoors and false inside an instance
  PASS  Say outdoors sends the WHOLE dump inside the click, not off timers
  PASS  Say INSIDE an instance keeps the stagger, because the restriction lifts there
  PASS  A whisper to nobody drops the rest of the dump and says so once
  PASS  An unrelated system message leaves a whisper dump alone
  PASS  A system message with no dump in flight is a cheap no
  PASS  A second send supersedes whatever the first still had queued
  PASS  Export.Send does nothing with nothing to send
  PASS  Export.SessionLabel asks the window, never the provider
  PASS  Export.SessionLabel survives a window whose label raises
  PASS  Export.SessionLabel names what a bare config can name on its own
  PASS  Export.Build goes through the aggregator and nowhere near the meter API
  PASS  Export.Build answers nil when there is no aggregator to ask
  PASS  Export.Open refuses to open at all while restricted
  PASS  Export.ResolveMetric answers the pinned stat
  PASS  Export.ResolveMetric ships pinned to a real stat, never to the empty string
  PASS  Export.ResolveMetric treats the old empty-string choice as unset
  PASS  Export.ResolveMetric treats a stat this build does not offer as unset
  PASS  Export.ResolveMetric falls back to the first catalog stat
  PASS  Export.ResolveMetric is callable through the colon form
  PASS  The Metric selector offers exactly the catalog, with no sentinel entry
  PASS  The modal's whisper row is a row, not an overlap
  PASS  Hiding the export modal closes an open dropdown menu (LibKa0s-Widgets-1.0)
  PASS  Hiding the export modal with no menu ever opened is a safe no-op
  PASS  Clicking a Metric row builds a real menu row and stores that metric
  PASS  Opening Channel after Metric repaints the pooled rows and stores a channel
  PASS  Export: the copy window comes from LibKa0s-Widgets-1.0
  PASS  Export: showing the copy window puts the text in it
  PASS  Export: the copy window is built once and reused
  PASS  Print to Chat re-checks the restriction at the click, not at the open
  PASS  Print to Chat names a blank whisper recipient before it builds anything
  PASS  Print to Chat asks the game for the target at the click
  PASS  Print to Chat whispers the player currently targeted
  PASS  Print to Chat says so rather than swallowing a click with nothing to export
  PASS  Print to Chat builds with the chosen metric as the SORT COLUMN
  PASS  Print to Chat leaves the lines themselves as the confirmation on SELF
  PASS  Print to Chat confirms a send that left the client, without counting the header
  PASS  Print to Chat warns BEFORE a Say dump the server may truncate
  PASS  Print to Chat does not warn where the stagger is available or the dump is short
  PASS  GetContext translates Blizzard's instance token to the setting's name
  PASS  A delve reads as `delve`, not as the scenario it reports itself to be
  PASS  An instance type this build has never heard of resolves to world
  PASS  A fresh profile shows the window everywhere and hides it nowhere
  PASS  The reason token is stable and unlocalized
  PASS  hideWhenSolo hides a window whose context already said yes
  PASS  hideInVehicle hides a window whose context already said yes
  PASS  Context is decided BEFORE the two vetoes, so the reason is the real one
  PASS  The vehicle answer is read live, because nothing on the bus announces it
  PASS  Each player-state rule hides its own state, and only when switched on
  PASS  A rule switched on with its state absent leaves the window alone
  PASS  A window whose rules predate these keys is not hidden by them
  PASS  Skyriding hides on capability, before the player has left the ground
  PASS  A druid travel form counts as mounted; a combat form does not
  PASS  hideInCombat and hideOutOfCombat each own one side of the pull
  PASS  Both combat rules on is a window that never shows, and says which side
  PASS  The combat answer is read live, with no latched flag to go stale
  PASS  Not one rule ships switched on
  PASS  Context is decided before EVERY veto, however many there are
  PASS  A window with no rules at all is allowed, not hidden
  PASS  A non-table window is refused by name
  PASS  Allows() is the same implementation under the ladder's name
  PASS  Evaluate records the last answer per window and counts the changes
  PASS  Refresh is Evaluate under the name a caller thinks in
  PASS  Evaluate publishes NOTHING
  PASS  Forget drops every remembered answer
  PASS  Evaluate copes with a database that is not up yet
  PASS  A window that should not show never reaches the provider at all
  PASS  The refusal lifts the moment the context does
  PASS  modules/Visibility.lua never touches a frame
  PASS  The combat rules use UnitAffectingCombat, never an InCombatLockdown proxy
  PASS  Visibility listens on the bus and registers no game event
  PASS  A profile change forgets the old answers and re-evaluates
  PASS  With every state live at once, the vetoes answer in one fixed order
  PASS  Every veto is decided before the combat pair, not after it
  PASS  Each context switched off hides with its own name, and only its own
  PASS  A context key is show-shaped; a veto key is hide-shaped
  PASS  Both gates read truthiness, not `== true`
  PASS  Anything that is not a table is `no window`, whatever it is
  PASS  A visibility field that is not a table reads as `no rules`, not as hide
  PASS  The master enable, test mode and perf suspend are NOT read here
  PASS  WindowManager is published under the flat name every caller uses
  PASS  Init builds one live instance per stored config, and is idempotent
  PASS  All() answers in the registry's order, which is the user's
  PASS  Resolve takes an id or a name, and folds case on the way in only
  PASS  A window literally named "2" wins over the window whose id is 2
  PASS  Create appends a window with the shipped defaults and a live instance
  PASS  Ids are monotonic and never reused
  PASS  A duplicate name is disambiguated rather than refused
  PASS  A window created with no name is numbered off how many windows exist
  PASS  The default number counts windows rather than climbing with deleted ids
  PASS  The default name steps past a name the player has already taken
  PASS  Rename stores the new name and refuses an empty one
  PASS  Duplicate deep-copies the source and offsets the copy
  PASS  Delete removes the config, destroys the instance and repoints the picker
  PASS  THE LAST WINDOW IS NOT DELETABLE
  PASS  An empty registry is re-seeded with a default
  PASS  Delete answers false for a window that is not there
  PASS  Every registry mutation announces WINDOWS_CHANGED, and nothing else does
  PASS  CopyFrom with no filter copies every group, deeply
  PASS  CopyFrom with ONE group key copies only that group
  PASS  CopyFrom accepts an array of keys and a set of them
  PASS  CopyFrom drops a group key this build does not know
  PASS  CopyFrom refuses a missing source or target, and no-ops onto itself
  PASS  CopyFrom re-normalizes the target's shape afterwards
  PASS  ResetPosition and ResetPositions put windows back in the middle
  PASS  ResetPosition defaults to the window the picker is pointed at
  PASS  SetLocked flips every window and touches NOTHING else
  PASS  IsLocked is false the moment any one window is unlocked
  PASS  SetTestMode routes through core/State.lua and marks every window dirty
  PASS  Toggle with no name flips every window; with a name, one
  PASS  MarkAllDirty costs one flag each and nothing else
  PASS  BuildListLines names every window and says whether it is on screen
  PASS  Suspend stops the coalescing timers without hiding anything
  PASS  Resume restores from CURRENT state: a window made while suspended comes back
  PASS  Minimap.Init creates a launcher and registers it against the live profile
  PASS  The launcher's name matches what the settings row looks it up by
  PASS  Minimap.Init is idempotent
  PASS  Minimap.Init adopts a broker object another path already created
  PASS  Init answers false, quietly, when LibDataBroker is absent
  PASS  Init answers false when LibDBIcon is absent
  PASS  Init answers false before the database exists
  PASS  Refresh before Init is a quiet no-op
  PASS  Refresh re-reads minimap.hide off the live profile
  PASS  Left-click toggles the windows through WindowManager's own seam
  PASS  Right-click opens the settings through OpenOptionsPanel, not a private path
  PASS  A click on a build with no window manager does nothing rather than raising
  PASS  The tooltip states BOTH clicks and the version
  PASS  The tooltip callback never shows or clears the tooltip itself
  PASS  The tooltip callback tolerates an object it cannot write to
  PASS  The profile ships the one key LibDBIcon reads, and nothing else
  PASS  modules/Minimap.lua passes the silent flag to every LibStub call
  PASS  Schema: a `hidden` row is writable and listable but draws no control
  PASS  Schema: the export choices are hidden from the panel but NOT from the seam
  PASS  The meta colour mode sets every bar and header in the window at once
  PASS  The meta colour mode leaves both TEXT surfaces alone
  PASS  The other three meta rows broadcast their own kind of setting
  PASS  A surface changed after the broadcast keeps its own answer
  PASS  The Frame page's Defaults button does NOT broadcast
  PASS  Schema: every page's tabs are the designed ones, in order, at the designed size
  PASS  Schema: the General page opens on Master controls, holding exactly the canonical set
  PASS  Schema: the master controls are ADDON-WIDE, and the per-window three are untouched
  PASS  Schema: a moved setting is declared ONCE, not twice
  PASS  Schema: every colour swatch has its mode beside it, on the same line
  PASS  Schema: NO colour row is ever disabled, and every one says why in words
  PASS  Schema: which class a colour means is DECLARED, not inferred from its path
  PASS  Schema: every row on every page carries a group, so no page renders untabbed
  PASS  Schema: a tab that mixes kinds of control names each kind between them
  PASS  Schema: a subgroup is CONTIGUOUS, or its heading prints twice
  PASS  Schema: no tab holds fewer than two controls
  PASS  Schema: a hidden row is filed under a tab that exists, and draws nothing
  PASS  Schema: every tab name and row label is a localized string, not a bare literal
  PASS  Schema: every Controls row carries its own icon in front of its words
  PASS  Schema: a control label degrades to its words when there is no art
  PASS  Schema: the active tab is session state and has no home in the schema
  PASS  Schema: the column header strip is styled on the page where columns are chosen
  PASS  Schema: the header controls are EDITED on Header and STORED under frame
  PASS  Schema: every group on every page is CONTIGUOUS, or a heading prints twice
  PASS  Schema: every LSM border setting is one this suite knows honours "None"
  PASS  Schema: every text surface offers face, outline, shadow and colour
  PASS  Schema: Highlight yourself and Alternating background ship OFF
  PASS  RestoreAllDefaults resets EVERY window, not just the selected one
  PASS  RestoreAllDefaults is the equivalent of a NEW PROFILE
  PASS  RestoreAllDefaults leaves the profile LIST alone
  PASS  A profile reset rebuilds through the ONE message, not by direct calls
  PASS  Schema: a window path resolves against the session's ACTIVE window
  PASS  Schema: a global path is unaffected by which window is active
  PASS  Schema: an unset active window falls back to the FIRST window, never to nil
  PASS  Schema: a stale active-window id falls back to the first window
  PASS  Schema: the inverted row stores the negation of what it displays
  PASS  SetByPath: refuses a value the row's validate() rejects, and stores nothing
  PASS  SetByPath: refuses a path that is not a row
  PASS  SetByPath: fires the row's onChange exactly once, with the value and window id
  PASS  SetByPath: onChange runs AFTER the write, never before
  PASS  SetByPath: logs the change exactly ONCE
  PASS  SetByPath: announces CONFIG_CHANGED once, tagged with the row's page and window
  PASS  SetByPath: re-syncs open panels IN PLACE, never structurally
  PASS  SetByPath: a table value is COPIED in, never stored by reference
  PASS  ApplyDefault: restores through the same seam, deep-copying a table default
  PASS  ApplyDefault: round-trips the inverted row back to its SHIPPED stored value
  PASS  SetByPath: window.columns repairs any array into the WHOLE catalog
  PASS  SetByPath: window.columns DROPS a statistic this build does not have
  PASS  SetByPath: window.columns keeps a repeated statistic's FIRST appearance only
  PASS  SetByPath: window.columns stores the enabled ones ahead of the disabled ones
  PASS  SetByPath: window.columns REBUILDS the array rather than adopting the caller's
  PASS  SetByPath: window.columns takes the same log, message and refresh a scalar takes
  PASS  SetByPath: window.columns is readable through the generic resolver
  PASS  SetByPath: window.columns REFUSES a non-table value, with a message
  PASS  SetByPath: window.columns REFUSES an empty array, with a message
  PASS  SetByPath: window.columns REFUSES a gap or a string key, with a message
  PASS  SetByPath: window.columns REFUSES an entry that is not a table, with a message
  PASS  SetByPath: window.columns REFUSES an array with nothing enabled, with a message
  PASS  SetByPath: window.columns REFUSES an array whose every statistic this build dropped, with a message
  PASS  SetByPath: a path INTO the column array is refused by name
  PASS  Schema: NS.NormalizeColumns is published for the migration ladder
  PASS  Schema defaults: the two trees the validator compares are both present
  PASS  Schema defaults: every non-session row resolves against defaults/Profile.lua
  PASS  Schema defaults: every row's default equals the shipped default, compared deeply
  PASS  Schema defaults: every color default agrees on all four channels
  PASS  Schema defaults: a table default is never the SAME table the defaults tree holds
  PASS  Schema defaults: session-only rows are exempt and carry their own storage
  PASS  ValidateSchema: reports zero failures on a healthy load
  PASS  ValidateSchema: counts a row whose default disagrees with the defaults tree
  PASS  ValidateSchema: counts a row whose path does not resolve
  PASS  ValidateSchema: compares a color CHANNEL, not just the presence of a table
  PASS  Slash: NS.COMMANDS entries are positional triples, not named fields
  PASS  Slash: no verb is declared twice
  PASS  Slash: every reserved verb is present, in the order the standard fixes
  PASS  Slash: the host verbs are declared and each carries a real handler
  PASS  Slash: `reset` takes a PATH, not a page
  PASS  Slash: every sub-verb a handler accepts is named in its own description
  PASS  Slash: an unknown verb says so and prints the help
  PASS  Slash: `options` is an alias for `config`, not a second command
  PASS  Slash: `version` reports the TOC's version rather than a hardcoded string
  PASS  Slash: `get` and `set` land on the addon's own schema seam
  PASS  Slash: `set` on a window path writes the ACTIVE window
  PASS  Slash: `reset <path>` restores exactly that one setting
  PASS  Slash: `resetall` restores every row
  PASS  Slash: `list` groups by the row's PAGE, the same key the panel pages use
  PASS  Slash: `perf` is declared in NS.COMMANDS and routed to NS.Perf.OnCommand
  PASS  Slash: `export` opens the modal on the window the player named
  PASS  Slash: `export` with no name falls back to a window rather than to nothing
  PASS  Slash: `export` names a window it cannot find rather than opening another
  PASS  Slash: `export` refuses while the game restricts combat data
  PASS  Slash: the library did not register `perf` behind the addon's back
  PASS  Slash: `lock` sets, and a bare `lock` toggles
  PASS  Slash: `test` sets and toggles through the registry
  PASS  Slash: `lock` moves the lock and NOTHING else
  PASS  Slash: `window new` and `window delete` act on the registry
  PASS  Slash: `window list` prints one line per window
  PASS  Slash: `window` with an unknown sub-verb prints the usage
  PASS  Slash: `toggle` reaches the registry and reports its refusal
  PASS  Slash: `reset-positions` moves every window and says how many
  PASS  Slash: `debug on` / `debug off` set the logging flag; a bare `debug` moves the window
  PASS  Slash: `debug tooltip` toggles the tooltip channel and says which way
  PASS  Slash: EVERY tooltip-channel line is behind the flag, not just some
  PASS  Slash: `debug tooltip` touches neither the logging flag nor the console
  PASS  Slash: `debug feign` with no argument prints the recording
  PASS  Slash: `debug feign of` names the rejected argument and leaves the trace alone
  PASS  Slash: `diag`, `recap` and `identity` each reach their OWN report and no other
  PASS  Slash: the three read verbs run with no debug console seam at all
  PASS  Slash: a read verb moves neither the console window nor the logging flag
  PASS  Slash: the debug sub-verb is matched case-insensitively
  PASS  Slash: `debug feign on` arms the recording and says exactly what to do next
  PASS  Slash: `debug feign off` stops the recording and says so
  PASS  Slash: the feign argument is case-folded, and a word after it is ignored
  PASS  Slash: a refused feign argument does not fall through to the console toggle
  PASS  Slash: a word the ladder does not know toggles the console, as a bare `debug` does
  PASS  Slash: with core/Diagnostics.lua absent the debug verbs go quiet, not through
  PASS  Slash: a Diagnostics too old to arm a trace reports off rather than promising one
  PASS  Slash: registration goes through AceConsole, on both tokens
  PASS  Slash: both registered tokens reach the SAME dispatcher
  PASS  Slash: no raw SLASH_* global is claimed anywhere
  PASS  Slash: Register is a no-op rather than a raise when there is no AceConsole
  PASS  Slash: /mm list heads each block with the page AND the tab
  PASS  Options: General is the FIRST page, above Windows
  PASS  Options: every window page is marked as nested, and the two that are not are not
  PASS  Options: the page HEADING keeps the plain name, mark or no mark
  PASS  Options: the parent category is registered at CreateOptionsPanel time
  PASS  Options: every page's subcategory is registered eagerly, before any panel is shown
  PASS  Options: a page's ctx carries its page key
  PASS  Options: the body is NOT built until the panel's first OnShow
  PASS  Options: a second OnShow does NOT re-render an already-rendered page
  PASS  Options: a hidden page is marked dirty and re-renders on its NEXT show
  PASS  Options: the Defaults button is built on first show, not at registration
  PASS  Options: EnsureDefaultsButton runs OUTSIDE the already-rendered guard
  PASS  Options: a page that declines a Defaults button never grows one
  PASS  Options: the Columns page's Defaults button restores the SHIPPED column list
  PASS  Options: the Columns page's Defaults button ALSO restores the window.columnHeader.* schema rows
  PASS  Options: the canvas footer's Defaults control reaches the same handler as the header button
  PASS  Options: opening the panel is REFUSED under combat lockdown, with a notice
  PASS  Options: a refused open is NOT deferred and replayed when combat ends
  PASS  Options: a page reached from the Blizzard sidebar mid-combat refuses to render
  PASS  Options: no settings page wires its own OnShow
  PASS  Options: the Profiles page refuses to render mid-combat, through the library's guard
  PASS  Options: a profile switch re-opens the Profiles page's AceConfigDialog
  PASS  Options: a widget's set() routes through NS.SetByPath
  PASS  Options: a checkbox's set() routes through NS.SetByPath too
  PASS  Options: applyDefault routes through NS.SetByPath, not around it
  PASS  Options: the panel and the CLI resolve a page's rows through the SAME function
  PASS  Options: skipRestoreAll vetoes the profiles page from a global reset
  PASS  Options: a global reset restores window POSITIONS, which no schema row owns
  PASS  Options: CreateOptionsPanel is idempotent
  PASS  Options: CreateOptionsPanel runs the schema validator
  PASS  Options: AceGUI is resolved once and published for the page builders
  PASS  Options: the shared LSM30_Border slot is re-registered once, above what AGSMW left in it
  PASS  Panel: every tabbed page opens on its first tab and draws a strip
  PASS  Panel: Profiles draws no strip
  PASS  Panel: switching tabs re-renders without leaving the previous tab's widgets behind
  PASS  Panel: the Master controls tab closes with the composer's two reset buttons
  PASS  Panel: the Statistic colors tab says where its colours are actually worn
  PASS  Panel: every window sub-page banners the active window, and Windows has no second picker
  PASS  Panel: choosing a window in the banner retargets every page and keeps the tab
  PASS  Blocks: one block per item, each carrying its index and its label
  PASS  Blocks: the glyph says enabled or disabled, and clicking it toggles
  PASS  Blocks: the glyph's tooltip says what the CLICK will do
  PASS  Blocks: a repaint gives every block back before it takes any out
  PASS  Blocks: a released block is off its AceGUI frame, not merely hidden
  PASS  Blocks: a reused block reports the index it now carries, not the one it was built with
  PASS  Blocks: a drag reports where it landed
  PASS  Blocks: the page hands LibKa0s the boundary, so a shown column stops at the rule
  PASS  Blocks: a hidden column has no handle, because its order means nothing
  PASS  Blocks: a repaint cancels the controller the render before it built
  PASS  Blocks: a drag that lands where it started reports nothing
  PASS  Blocks: an enabled block cannot be dragged past the last enabled one
  PASS  Blocks: a list with nothing disabled drags end to end
  PASS  Blocks: this file draws NO row background of its own
  PASS  Blocks: the library's box is behind every row, muted for a hidden column
  PASS  Blocks: the handle's gutter is the library's, never restated here
  PASS  Blocks: the rule is drawn once, under the last enabled block
  PASS  Blocks: a spec that is not a table is refused, and refused without touching the page
  PASS  Blocks: with AceGUI absent the page refuses rather than half-drawing
  PASS  Blocks: an empty item list still builds and finishes a controller
  PASS  Blocks: the boundary is a COUNT of enabled items, never a scan for the first disabled
  PASS  Blocks: the rule is an empty AceGUI Heading, sitting after the boundary block
  PASS  Blocks: no rule is drawn when there is no divide to mark, at either end
  PASS  Blocks: the label is gold when the column is shown and grey when it is not
  PASS  Blocks: the carried copy says the same thing the row does
  PASS  Blocks: every hidden row is registered, so the indices the library moves are the real ones
  PASS  Blocks: the geometry handed to the library is the published pair, not a private copy
  PASS  Blocks: the handle offers the localized drag tooltip, and the catalogued icon
  PASS  Blocks: the glyph sits clear of the library's handle gutter
  PASS  Blocks: a reused block comes back at full alpha
  PASS  Blocks: an item with no label draws an empty string, not a nil
  PASS  Blocks: CancelReorder survives a page that never rendered a list
  PASS  Blocks: cancelling twice releases the blocks once
  PASS  Blocks: the release is announced on the debug log, with the count
  PASS  Blocks: the returned list and the list parked on the ctx are two tables, same contents
  PASS  Columns: the page draws one block per statistic in the catalog
  PASS  Columns: the blocks are in the STORED order, ticked ones first
  PASS  Columns: unticking a column drops it to the TOP of the disabled group
  PASS  Columns: ticking a statistic adds it as the RIGHTMOST column
  PASS  Columns: the last shown column cannot be unticked, and the refusal is said
  PASS  Columns: dragging a block reorders the stored array
  PASS  Columns: a drag that goes nowhere writes nothing
  PASS  Columns: no change is applied under combat lockdown, and the reason is said
  PASS  Columns: a refused write is REPORTED and the page is not repainted
  PASS  Columns: an accepted write IS repainted
  PASS  Columns: the stored array is never the page's own working copy
  PASS  Degraded: the library really is absent, so every case below is measuring a stub
  PASS  Degraded: every seam soft-optionals its major, so a missing library is not a load error
  PASS  Degraded: core/CoreSetup.lua takes its fallback and the printer still works
  PASS  Degraded: the 'not installed' line is said ONCE, on the first line the addon prints
  PASS  Degraded: the secret-safe probe survives, because this addon's values are secret
  PASS  Degraded: the window chrome degrades to NOTHING rather than to a hand-copied backdrop
  PASS  Degraded: the stored-color reader does NOT degrade to nothing
  PASS  Degraded: core/PerfSetup.lua takes its fallback and every bracket short-circuits
  PASS  Degraded: core/DebugLogSetup.lua takes its fallback and the flag still works
  PASS  Degraded: settings/Slash.lua takes its fallback
  PASS  Degraded: settings/OptionsSetup.lua takes its fallback and says the panel is unavailable
  PASS  Degraded: `/mm` with no arguments still prints help
  PASS  Degraded: every declared verb is reachable and none of them raises
  PASS  Degraded: the schema verbs NAME the missing library rather than going quiet
  PASS  Degraded: the host verbs are untouched, because they never went to the library
  PASS  Degraded: an unknown verb is named and followed by help
  PASS  Degraded: `/mm version` still answers with the packaged version
  PASS  Degraded: the options stub publishes every Helpers member the page files touch
  PASS  Degraded: LSMValues keeps its DEFERRED shape and never answers an empty list
  PASS  Degraded: reset-everything still works, and still refuses to touch the Profiles page
  PASS  Degraded: the schema row count is UNCHANGED versus a full load, bar the composed blocks
  PASS  Degraded: the schema is the same rows, path for path and page for page
  PASS  Degraded: every schema page is registered as an options page on both paths
  PASS  Degraded: the namespace publishes the same seam members with and without the library
  PASS  Degraded: every NS.Perf member the addon actually reaches exists on the stub
  PASS  Degraded: the export modal refuses to open with no dropdown widget
  PASS  Degraded: the addon still enables end to end with no library
  PASS  parity: the Options stub carries every public member of the live Helpers surface
  PASS  eol: every tracked file carries the terminator .gitattributes declares for it

1748 passed, 0 failed, 0 skipped, 1748 total
